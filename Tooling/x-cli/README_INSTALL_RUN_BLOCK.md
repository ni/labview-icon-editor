## Install & Run (Linux)

```bash
# Download the single-file binary from the GitHub Release assets
# e.g., asset name: XCli-linux-x64
chmod +x ./XCli-linux-x64
./XCli-linux-x64 --version
./XCli-linux-x64 --help
```
