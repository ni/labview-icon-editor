# Test Report (Concise)
- **Build/Run**: CI run `<id>` on commit `<sha>`.
- **Coverage (total)**: **≥75% required** — see Cobertura and HTML artifacts.
- **Per-file floors**: as in `docs/compliance/coverage-thresholds.json`.
- **Outcome**: `<Pass/Fail>`; list any files under floor.
- **Artifacts**:
  - HTML ZIP: `artifacts/coverage-html.zip` (contains `index.html`)
  - XML: `coverage.xml`
