using System;

namespace XCli.ViAnalyzer;

// Minimal stub so publish produces XCli.exe without full dependencies.
public static class ViAnalyzerRunCommand
{
    public static int Run(string[] args)
    {
        Console.WriteLine("[x-cli] vi-analyzer-run stub: success");
        return 0;
    }
}
