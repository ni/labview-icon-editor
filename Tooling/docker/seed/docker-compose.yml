services:
  seed:
    build:
      context: ../../seed
      dockerfile: Dockerfile
    image: seed-local:latest
    pull_policy: never
    stdin_open: true
    tty: true
    volumes:
      - ${WORKSPACE_HOST_PATH:-../../..}:/workspace
    working_dir: /workspace
    command: ["seed", "--help"]
