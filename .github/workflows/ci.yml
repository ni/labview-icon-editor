name: CI Pipeline
run-name: "CI ${{ github.sha }} (#${{ github.run_id }}.${{ github.run_attempt }})"

permissions:
  actions: read
  contents: read
  pull-requests: read

env:
  LVIE_RUN_ID: ${{ github.run_id }}-${{ github.run_attempt }}-${{ github.sha }}
  LVIE_EXPECTED_RUNNER_LABEL: ${{ vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
  LVIE_RUNNER_LABEL_64: ${{ vars.LVIE_RUNNER_LABEL_64 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
  LVIE_RUNNER_LABEL_32: ${{ vars.LVIE_RUNNER_LABEL_32 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
  LVIE_CANONICAL_RUNNER_LABEL: self-hosted-windows-lv
  LVIE_FORCE_NO_LABVIEW_DEVMODE: 1
  LVIE_RUNNER_ACL_CHECK: 1
  LVIE_RUNNER_ACL_AUTOFIX: 0
  LVIE_RUNNER_ACL_WARN_ONLY: 1
  LVIE_REQUIRE_RUNNER_CLI: ${{ vars.LVIE_REQUIRE_RUNNER_CLI || '0' }}
  LVIE_VERIFY_IEPATHS_ENFORCE: ${{ vars.LVIE_VERIFY_IEPATHS_ENFORCE || '0' }}
  LVIE_PYLAVI_BASELINE_PATH: ${{ vars.LVIE_PYLAVI_BASELINE_PATH || '' }}
  LVIE_PYLAVI_BASELINE_REQUIRED: ${{ vars.LVIE_PYLAVI_BASELINE_REQUIRED || '0' }}
  LVIE_PYLAVI_FAIL_ON_DELTA: ${{ vars.LVIE_PYLAVI_FAIL_ON_DELTA || '0' }}

concurrency:
  group: labview-${{ github.repository }}-${{ vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

on:
  push:
    branches:
      - main
      - develop
      - release/*
  pull_request:
    branches:
      - main
      - develop
      - release/*
      - feature/*
      - hotfix/*
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  workflow_dispatch:
    inputs:
      dispatch_tag:
        description: "Expected tag for this run (optional, for dispatched builds)"
        required: false
        type: string
      allow_baseline_update:
        description: "Allow PSScriptAnalyzer baseline updates (manual runs only)"
        required: false
        type: boolean
      expected_sha:
        description: "Expected commit SHA for this run (optional, for dispatched builds)"
        required: false
        type: string
      strict_sha:
        description: "Require expected_sha to be provided and matched"
        required: false
        type: boolean
      publish_prerelease:
        description: "Publish prerelease for this dispatch run"
        required: false
        default: false
        type: boolean
      force_gcli_lunit:
        description: "Selects the release-priority CI profile for workflow_dispatch routing"
        required: false
        default: false
        type: boolean
      vipc_apply_info:
        description: "Run informational VIPC apply diagnostics after audit (non-blocking)"
        required: false
        default: false
        type: boolean
      source_test_mode:
        description: "Source-test failure mode for workflow_dispatch only (inherit|strict|canary)"
        required: false
        default: inherit
        type: choice
        options:
          - inherit
          - strict
          - canary
      source_test_labview_year_override:
        description: "Diagnostics-only LabVIEW year override for source tests (workflow_dispatch only, e.g. 2026)"
        required: false
        type: string

jobs:
  # Stage 1: Run identity and publish-eligibility context.
  run-metadata:
    name: Run Metadata
    runs-on: ubuntu-latest
    outputs:
      run_uid: ${{ steps.meta.outputs.run_uid }}
      sha: ${{ steps.meta.outputs.sha }}
    steps:
      - id: meta
        name: Generate run manifest
        shell: pwsh
        run: |
          $runId = '${{ github.run_id }}'
          $runAttempt = '${{ github.run_attempt }}'
          $sha = '${{ github.sha }}'
          $ref = '${{ github.ref }}'
          $headRef = '${{ github.head_ref }}'
          $eventName = '${{ github.event_name }}'
          $actor = '${{ github.actor }}'
          $repo = '${{ github.repository }}'
          $workflow = '${{ github.workflow }}'
          $expectedSha = '${{ github.event.inputs.expected_sha }}'
          $strictSha = '${{ github.event.inputs.strict_sha }}'
          $publishPrerelease = '${{ github.event.inputs.publish_prerelease }}'
          $runUid = "{0}-{1}-{2}" -f $runId, $runAttempt, $sha

          if ($eventName -eq 'workflow_dispatch' -and $publishPrerelease -eq 'true' -and $strictSha -ne 'true') {
            throw "strict_sha=true is required when publish_prerelease=true."
          }
          if ($eventName -eq 'workflow_dispatch' -and $publishPrerelease -eq 'true' -and [string]::IsNullOrWhiteSpace($expectedSha)) {
            throw "expected_sha is required when publish_prerelease=true."
          }
          if ($strictSha -eq 'true' -and [string]::IsNullOrWhiteSpace($expectedSha)) {
            throw "expected_sha is required when strict_sha=true."
          }
          if (-not [string]::IsNullOrWhiteSpace($expectedSha) -and $expectedSha -ne $sha) {
            throw ("expected_sha '{0}' does not match github.sha '{1}'." -f $expectedSha, $sha)
          }

          $manifest = [pscustomobject]@{
            run_id        = $runId
            run_attempt   = $runAttempt
            run_uid       = $runUid
            sha           = $sha
            ref           = $ref
            head_ref      = $headRef
            event         = $eventName
            actor         = $actor
            repository    = $repo
            workflow      = $workflow
            timestamp_utc = (Get-Date).ToUniversalTime().ToString('o')
          }

          $path = Join-Path $env:RUNNER_TEMP 'run-manifest.json'
          $manifest | ConvertTo-Json -Depth 6 | Out-File -FilePath $path -Encoding ascii
          "run_uid=$runUid" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "sha=$sha" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "manifest_path=$path" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append

      - name: Upload run manifest
        uses: actions/upload-artifact@v4
        with:
          name: run-manifest
          path: ${{ steps.meta.outputs.manifest_path }}

  prerelease-context:
    name: Pre-Release Context
    needs: run-metadata
    runs-on: ubuntu-latest
    outputs:
      should_publish: ${{ steps.ctx.outputs.should_publish }}
      publish_mode: ${{ steps.ctx.outputs.publish_mode }}
      publish_reason: ${{ steps.ctx.outputs.publish_reason }}
      ci_profile: ${{ steps.ctx.outputs.ci_profile }}
      release_priority_mode: ${{ steps.ctx.outputs.release_priority_mode }}
      bump_type_override: ${{ steps.ctx.outputs.bump_type_override }}
      merged_pr_number: ${{ steps.ctx.outputs.merged_pr_number }}
      bump_conflict: ${{ steps.ctx.outputs.bump_conflict }}
      bump_conflict_labels: ${{ steps.ctx.outputs.bump_conflict_labels }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - id: ctx
        name: Resolve publish eligibility and bump override
        uses: actions/github-script@v7
        env:
          PUBLISH_PRERELEASE: ${{ github.event.inputs.publish_prerelease || 'false' }}
          FORCE_GCLI_LUNIT: ${{ github.event.inputs.force_gcli_lunit || 'false' }}
        with:
          script: |
            const path = require('path');
            const {
              loadLabelContract,
              buildReleaseLabelMap,
              normalizeLabels,
            } = require(path.join(process.env.GITHUB_WORKSPACE || process.cwd(), '.github', 'scripts', 'label-metadata.js'));
            const contractPath = path.join(process.env.GITHUB_WORKSPACE || process.cwd(), '.github', 'labels', 'label-contract.json');
            const labelContract = loadLabelContract(contractPath);
            const releaseLabelMap = buildReleaseLabelMap(labelContract);
            const publishInput = (process.env.PUBLISH_PRERELEASE || 'false').toLowerCase() === 'true';
            const forceGcliLunitInput = (process.env.FORCE_GCLI_LUNIT || 'false').toLowerCase() === 'true';
            const ciProfile = context.eventName === 'workflow_dispatch' && forceGcliLunitInput
              ? 'release-priority'
              : (context.eventName === 'pull_request' ? 'pr-fast' : 'full');
            const releasePriorityMode = ciProfile === 'release-priority';

            async function getCommitParentCount() {
              const response = await github.rest.repos.getCommit({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: context.sha,
              });
              const parents = Array.isArray(response.data.parents) ? response.data.parents : [];
              return parents.length;
            }

            async function findMergedPrsTargetingDevelop() {
              const response = await github.rest.repos.listPullRequestsAssociatedWithCommit({
                owner: context.repo.owner,
                repo: context.repo.repo,
                commit_sha: context.sha,
              });
              const mergedDevelopPrs = response.data
                .filter((pr) => pr.merged_at && pr.base && pr.base.ref === 'develop')
                .sort((a, b) => new Date(b.merged_at) - new Date(a.merged_at));
              return mergedDevelopPrs;
            }

            async function findRecentSuccessfulFullProfileRun(maxAgeHours) {
              const cutoffMs = Date.now() - (maxAgeHours * 60 * 60 * 1000);
              let page = 1;
              while (page <= 5) {
                const response = await github.rest.actions.listWorkflowRuns({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  workflow_id: 'ci.yml',
                  branch: 'develop',
                  event: 'push',
                  status: 'completed',
                  per_page: 100,
                  page,
                });
                const runs = Array.isArray(response.data.workflow_runs) ? response.data.workflow_runs : [];
                if (!runs.length) {
                  break;
                }

                for (const run of runs) {
                  const createdMs = Date.parse(run.created_at || '');
                  if (!Number.isFinite(createdMs)) {
                    continue;
                  }
                  if (createdMs < cutoffMs) {
                    return null;
                  }
                  if (run.id === context.runId) {
                    continue;
                  }
                  if (String(run.conclusion || '') === 'success') {
                    return run;
                  }
                }

                const datedRuns = runs
                  .map((run) => Date.parse(run.created_at || ''))
                  .filter((value) => Number.isFinite(value));
                if (datedRuns.length > 0 && Math.min(...datedRuns) < cutoffMs) {
                  break;
                }
                page += 1;
              }

              return null;
            }

            function findMergedPrForCurrentSha(mergedDevelopPrs) {
              const sha = String(context.sha || '').toLowerCase();
              for (const pr of mergedDevelopPrs) {
                const mergeSha = String(pr.merge_commit_sha || '').toLowerCase();
                if (mergeSha === sha) {
                  return pr;
                }
              }
              return null;
            }

            function deriveBumpFromLabels(pr) {
              const labels = (pr.labels || []).map((l) => String(l.name || ''));
              const found = normalizeLabels(labels, releaseLabelMap);
              if (found.normalized.length > 1) {
                return {
                  conflict: true,
                  conflictLabels: found.matchedRaw.join(', '),
                  bumpTypeOverride: '',
                };
              }
              return {
                conflict: false,
                conflictLabels: '',
                bumpTypeOverride: found.normalized[0] || 'patch',
              };
            }

            let shouldPublish = false;
            let publishMode = 'none';
            let publishReason = 'not-eligible';
            let bumpTypeOverride = '';
            let mergedPrNumber = '';
            let bumpConflict = false;
            let bumpConflictLabels = '';

            const commitParentCount = await getCommitParentCount();
            const isMergeCommit = commitParentCount > 1;
            const mergedDevelopPrs = await findMergedPrsTargetingDevelop();
            const hasMergedDevelopPrAssociation = mergedDevelopPrs.length > 0;
            const mergedPr = findMergedPrForCurrentSha(mergedDevelopPrs);
            if (mergedPr) {
              mergedPrNumber = String(mergedPr.number);
            }

            if (context.eventName === 'push' && context.ref === 'refs/heads/develop') {
              publishMode = 'auto';
              if (!isMergeCommit) {
                shouldPublish = false;
                publishReason = 'develop-push-not-merge-commit';
              } else if (mergedPr) {
                shouldPublish = true;
                publishReason = 'auto-eligible-develop-push-merged-pr-merge-commit';
              } else if (hasMergedDevelopPrAssociation) {
                shouldPublish = false;
                publishReason = 'develop-push-merged-pr-sha-mismatch';
              } else {
                shouldPublish = false;
                publishReason = 'develop-push-no-merged-pr-association';
              }
            } else if (context.eventName === 'workflow_dispatch') {
              publishMode = 'manual';
              if (!publishInput) {
                shouldPublish = false;
                publishReason = 'manual-intent-false';
              } else if (!isMergeCommit) {
                core.setFailed('Manual prerelease publish requires expected_sha to reference a merged develop merge commit.');
                return;
              } else if (!mergedPr) {
                if (hasMergedDevelopPrAssociation) {
                  core.setFailed('Manual prerelease publish requires expected_sha to match the merged develop PR merge SHA.');
                } else {
                  core.setFailed('Manual prerelease publish requires expected_sha to be associated with a merged PR targeting develop.');
                }
                return;
              } else {
                if (ciProfile === 'release-priority') {
                  const freshnessHours = 24;
                  let recentFullRun = null;
                  try {
                    recentFullRun = await findRecentSuccessfulFullProfileRun(freshnessHours);
                  } catch (error) {
                    core.setFailed(`Release-priority prerelease publish could not verify recent full-profile run freshness: ${error.message}`);
                    return;
                  }
                  if (!recentFullRun) {
                    core.setFailed(`Release-priority prerelease publish requires a successful full profile run on develop within the last ${freshnessHours} hours.`);
                    return;
                  }
                }
                shouldPublish = true;
                publishReason = 'manual-intent-eligible-merged-pr-merge-commit';
              }
            } else if (context.eventName === 'pull_request') {
              publishMode = 'pull-request';
              shouldPublish = false;
              publishReason = 'pull-request-events-never-publish';
            } else {
              publishMode = context.eventName;
              shouldPublish = false;
              publishReason = 'event-not-eligible';
            }

            const shouldDeriveMergedPrBump =
              (context.eventName === 'push' && context.ref === 'refs/heads/develop' && !!mergedPr) ||
              (context.eventName === 'workflow_dispatch' && publishInput && shouldPublish && !!mergedPr);
            if (shouldDeriveMergedPrBump) {
              const bumpInfo = deriveBumpFromLabels(mergedPr);
              bumpConflict = bumpInfo.conflict;
              bumpConflictLabels = bumpInfo.conflictLabels;
              bumpTypeOverride = bumpInfo.bumpTypeOverride;
            }

            core.setOutput('should_publish', shouldPublish ? 'true' : 'false');
            core.setOutput('publish_mode', publishMode);
            core.setOutput('publish_reason', publishReason);
            core.setOutput('ci_profile', ciProfile);
            core.setOutput('release_priority_mode', releasePriorityMode ? 'true' : 'false');
            core.setOutput('bump_type_override', bumpTypeOverride);
            core.setOutput('merged_pr_number', mergedPrNumber);
            core.setOutput('bump_conflict', bumpConflict ? 'true' : 'false');
            core.setOutput('bump_conflict_labels', bumpConflictLabels);

  runner-cli-build:
    name: Build runner-cli (linux-x64)
    needs: run-metadata
    uses: ./.github/workflows/runner-cli-reusable.yml
    permissions:
      actions: read
      contents: read
    with:
      rid: linux-x64
      artifact_name: runner-cli-linux-x64

  runner-cli-build-win:
    name: Build runner-cli (win-x64)
    needs: run-metadata
    uses: ./.github/workflows/runner-cli-reusable.yml
    permissions:
      actions: read
      contents: read
    with:
      rid: win-x64
      artifact_name: runner-cli-win-x64

  version-gate:
    name: LabVIEW Version Gate
    needs: [run-metadata, runner-cli-build]
    runs-on: ubuntu-latest
    outputs:
      raw: ${{ steps.lv.outputs.raw }}
      year: ${{ steps.lv.outputs.year }}
      minor: ${{ steps.lv.outputs.minor }}
      numeric: ${{ steps.lv.outputs.numeric }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Download runner-cli artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.runner-cli-build.outputs.artifact_name }}
          path: ${{ runner.temp }}/runner-cli
      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.418'
      - id: lv
        name: Assert .lvversion
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $repoRoot = $env:GITHUB_WORKSPACE
          $cliPath = Join-Path $env:RUNNER_TEMP 'runner-cli' 'runner-cli'
          if (Test-Path $cliPath) {
            Write-Host "Using prebuilt runner-cli artifact."
            chmod +x $cliPath
            $json = & $cliPath version-gate --repo-root $repoRoot --json
            $exitCode = $LASTEXITCODE
            if ($exitCode -ne 0) {
              exit $exitCode
            }
            $info = $json | ConvertFrom-Json
          } else {
            $runnerCliProject = Join-Path $repoRoot 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
            $canUseRunnerCli = (Get-Command dotnet -ErrorAction SilentlyContinue) -and (Test-Path $runnerCliProject)
            if ($canUseRunnerCli) {
            Write-Host "Using runner-cli version-gate."
            dotnet build $runnerCliProject --configuration Release | Out-Host
            if ($LASTEXITCODE -ne 0) {
              exit $LASTEXITCODE
            }
            $runnerCliDll = Join-Path (Split-Path $runnerCliProject) 'bin/Release/net8.0/runner-cli.dll'
            if (-not (Test-Path $runnerCliDll)) {
              throw "Runner CLI build succeeded but binary not found: $runnerCliDll"
            }
            $json = & dotnet $runnerCliDll version-gate --repo-root $repoRoot --json
            $exitCode = $LASTEXITCODE
            if ($exitCode -ne 0) {
              exit $exitCode
            }
            $info = $json | ConvertFrom-Json
            } else {
              if ($env:LVIE_REQUIRE_RUNNER_CLI -eq '1') {
                throw "LVIE_REQUIRE_RUNNER_CLI=1 but runner-cli was not available."
              }
              Write-Warning "runner-cli unavailable; falling back to Assert-LabVIEWVersion.ps1."
              $info = & "$repoRoot/Tooling/Assert-LabVIEWVersion.ps1" -RepoRoot $repoRoot -Context 'ci-version-gate' -WriteSummary
            }
          }
          Write-Warning ("labview_version not provided; using .lvversion ({0})." -f $info.Raw)
          "raw=$($info.Raw)" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "year=$($info.Year)" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "minor=$($info.MinorRevision)" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "numeric=$($info.NumericVersion)" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          Write-Host ("LabVIEW version gate passed: {0}" -f $info.Raw)

  core-conformance-hosted-linux:
    name: Core Conformance Evidence (Hosted Linux)
    needs: [run-metadata, version-gate, runner-cli-build]
    runs-on: ubuntu-latest
    outputs:
      evidence: ${{ steps.evidence.outputs.value }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Download runner-cli artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.runner-cli-build.outputs.artifact_name }}
          path: ${{ runner.temp }}/runner-cli
      - id: evidence
        name: Run Core JSON checks (linux)
        shell: bash
        run: |
          set -euo pipefail
          cli="${RUNNER_TEMP}/runner-cli/runner-cli"
          if [ ! -x "$cli" ]; then
            chmod +x "$cli"
          fi
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          evidence="core-linux:${run_url}:attempt-${GITHUB_RUN_ATTEMPT}"
          "$cli" version-gate --repo-root "$GITHUB_WORKSPACE" --json > "${RUNNER_TEMP}/core-linux-version-gate.json"
          "$cli" pylavi summarize --path "$GITHUB_WORKSPACE/Tooling/pylavi/fixtures/pylavi-offenders.sample.json" --json > "${RUNNER_TEMP}/core-linux-pylavi-summarize.json"
          cat > "${RUNNER_TEMP}/core-conformance-linux-evidence.json" <<JSON
          {
            "platform": "linux",
            "run_url": "${run_url}",
            "evidence": "${evidence}",
            "version_gate_json": "${RUNNER_TEMP}/core-linux-version-gate.json",
            "pylavi_summarize_json": "${RUNNER_TEMP}/core-linux-pylavi-summarize.json"
          }
          JSON
          echo "value=${evidence}" >> "$GITHUB_OUTPUT"
      - name: Upload Core conformance linux evidence
        uses: actions/upload-artifact@v4
        with:
          name: core-conformance-linux-evidence
          path: |
            ${{ runner.temp }}/core-linux-version-gate.json
            ${{ runner.temp }}/core-linux-pylavi-summarize.json
            ${{ runner.temp }}/core-conformance-linux-evidence.json
          if-no-files-found: error

  core-conformance-hosted-windows:
    name: Core Conformance Evidence (Hosted Windows)
    needs: [run-metadata, version-gate, runner-cli-build-win]
    runs-on: windows-latest
    outputs:
      evidence: ${{ steps.evidence.outputs.value }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Download runner-cli artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.runner-cli-build-win.outputs.artifact_name }}
          path: ${{ runner.temp }}/runner-cli
      - id: evidence
        name: Run Core JSON checks (windows)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $cli = Join-Path $env:RUNNER_TEMP 'runner-cli/runner-cli.exe'
          if (-not (Test-Path $cli)) {
            throw "runner-cli artifact missing at $cli"
          }
          $runUrl = "{0}/{1}/actions/runs/{2}" -f $env:GITHUB_SERVER_URL, $env:GITHUB_REPOSITORY, $env:GITHUB_RUN_ID
          $evidence = "core-windows:${runUrl}:attempt-$($env:GITHUB_RUN_ATTEMPT)"
          $versionOut = Join-Path $env:RUNNER_TEMP 'core-windows-version-gate.json'
          $pylaviOut = Join-Path $env:RUNNER_TEMP 'core-windows-pylavi-summarize.json'
          $evidenceOut = Join-Path $env:RUNNER_TEMP 'core-conformance-windows-evidence.json'
          & $cli version-gate --repo-root $env:GITHUB_WORKSPACE --json | Out-File -FilePath $versionOut -Encoding utf8
          if ($LASTEXITCODE -ne 0) {
            throw "runner-cli version-gate failed with exit code $LASTEXITCODE"
          }
          $sample = Join-Path $env:GITHUB_WORKSPACE 'Tooling/pylavi/fixtures/pylavi-offenders.sample.json'
          & $cli pylavi summarize --path $sample --json | Out-File -FilePath $pylaviOut -Encoding utf8
          if ($LASTEXITCODE -ne 0) {
            throw "runner-cli pylavi summarize failed with exit code $LASTEXITCODE"
          }
          $payload = [ordered]@{
            platform = 'windows'
            run_url = $runUrl
            evidence = $evidence
            version_gate_json = $versionOut
            pylavi_summarize_json = $pylaviOut
          }
          $payload | ConvertTo-Json -Depth 5 | Out-File -FilePath $evidenceOut -Encoding utf8
          "value=$evidence" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append -Encoding utf8
      - name: Upload Core conformance windows evidence
        uses: actions/upload-artifact@v4
        with:
          name: core-conformance-windows-evidence
          path: |
            ${{ runner.temp }}/core-windows-version-gate.json
            ${{ runner.temp }}/core-windows-pylavi-summarize.json
            ${{ runner.temp }}/core-conformance-windows-evidence.json
          if-no-files-found: error

  conformance-full:
    name: Conformance Check (Full, Strict)
    needs: [run-metadata, core-conformance-hosted-linux, core-conformance-hosted-windows, runner-cli-build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Download runner-cli artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.runner-cli-build.outputs.artifact_name }}
          path: ${{ runner.temp }}/runner-cli
      - name: Run full conformance gate
        shell: pwsh
        env:
          RC_HOSTED_LINUX_EVIDENCE: ${{ needs.core-conformance-hosted-linux.outputs.evidence }}
          RC_HOSTED_WINDOWS_EVIDENCE: ${{ needs.core-conformance-hosted-windows.outputs.evidence }}
        run: |
          $ErrorActionPreference = 'Stop'
          $cli = Join-Path $env:RUNNER_TEMP 'runner-cli/runner-cli'
          if (-not (Test-Path $cli)) {
            throw "runner-cli artifact missing at $cli"
          }
          chmod +x $cli
          $outDir = Join-Path $env:RUNNER_TEMP 'conformance-full'
          New-Item -Path $outDir -ItemType Directory -Force | Out-Null
          $jsonPath = Join-Path $outDir 'conformance-full.json'
          $json = & $cli conformance check --profile full --json --strict --repo-root $env:GITHUB_WORKSPACE
          $exitCode = $LASTEXITCODE
          $json | Out-File -FilePath $jsonPath -Encoding utf8
          if ($exitCode -ne 0) {
            throw "runner-cli conformance check failed with exit code $exitCode"
          }
          $result = $json | ConvertFrom-Json
          Write-Host ("Conformance profile: {0}" -f $result.profile)
          Write-Host ("Conformance totals: total={0}, pass={1}, warn={2}, fail={3}" -f $result.summary.total, $result.summary.pass, $result.summary.warn, $result.summary.fail)
      - name: Upload full conformance artifact
        uses: actions/upload-artifact@v4
        with:
          name: conformance-full
          path: ${{ runner.temp }}/conformance-full/conformance-full.json
          if-no-files-found: error

  powershell-lint:
    name: PowerShell Lint
    needs: run-metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Guard PSScriptAnalyzer baseline
        shell: pwsh
        env:
          ALLOW_BASELINE_UPDATE: ${{ github.event.inputs.allow_baseline_update }}
        run: |
          $baselinePath = 'Tooling/PSScriptAnalyzerBaseline.json'
          $eventName = '${{ github.event_name }}'
          $branchName = if ($eventName -eq 'pull_request') { '${{ github.head_ref }}' } else { '${{ github.ref_name }}' }
          $allowInput = $env:ALLOW_BASELINE_UPDATE -eq 'true'
          $allowBranch = $branchName -match '^lint-baseline/' -or $branchName -match '^chore/lint-baseline/'
          if ($allowInput -or $allowBranch) {
            Write-Host ("Baseline guard: update allowed (allow_input={0}, branch={1})." -f $allowInput, $branchName)
            exit 0
          }

          $baseSha = if ($eventName -eq 'pull_request') { '${{ github.event.pull_request.base.sha }}' } else { '${{ github.event.before }}' }
          $headSha = '${{ github.sha }}'
          if ([string]::IsNullOrWhiteSpace($baseSha) -or $baseSha -eq '0000000000000000000000000000000000000000') {
            $revList = @(git rev-list --max-count=2 $headSha)
            if ($revList.Count -gt 1) { $baseSha = $revList[1] }
          }
          if ([string]::IsNullOrWhiteSpace($baseSha)) {
            throw "Baseline guard: unable to determine base commit for comparison."
          }

          $baselineInBase = & git ls-tree -r --name-only $baseSha -- $baselinePath
          if (-not $baselineInBase) {
            Write-Host ("Baseline guard: baseline missing in base {0}; allowing initial baseline addition." -f $baseSha)
            exit 0
          }

          $changed = git diff --name-only $baseSha $headSha -- $baselinePath
          if ($changed) {
            throw ("PSScriptAnalyzer baseline change detected in {0}. Use branch prefix 'lint-baseline/' or workflow_dispatch input allow_baseline_update=true." -f $baselinePath)
          }

          Write-Host "Baseline guard: no changes detected."
      - name: Validate path root contract
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Test-PathContract.ps1" -WriteSummary
      - name: Validate CI selector/dev-mode contract
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Test-CiPipelineSelectorDevModeContract.ps1" -WriteSummary
      - name: Validate VI Analyzer container contract
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Test-ViAnalyzerContainerContract.ps1" -WriteSummary
      - name: Validate solo-maintainer workflow contract
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Test-SoloMaintainerWorkflowContract.ps1" -WriteSummary
      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: |
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted
          Install-Module PSScriptAnalyzer -Scope CurrentUser -Force -AllowClobber
      - name: Run PSScriptAnalyzer
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Invoke-PSScriptAnalyzer.ps1" -WriteSummary

  vip-prerelease-requirements-lint:
    name: VIP Prerelease Requirements Lint
    needs: run-metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Lint VIP prerelease requirements v1
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Test-VipPrereleaseRequirementsV1.ps1" -WriteSummary
      - name: Upload VIP prerelease requirements lint status
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vip-prerelease-requirements-lint
          path: builds/status/vip-prerelease-requirements-lint.json
          if-no-files-found: error

  docs-lint:
    name: Documentation Lint
    needs: run-metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run markdownlint
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Invoke-MarkdownLint.ps1" -WriteSummary
      - name: Validate CI docs source-of-truth contract
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE/Tooling/Test-CiDocsSourceOfTruth.ps1" -WriteSummary

  pylavi-validate:
    name: Validate LabVIEW Files (pylavi, report-only)
    needs: [run-metadata, version-gate, runner-cli-build-win]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Download runner-cli artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.runner-cli-build-win.outputs.artifact_name }}
          path: ${{ runner.temp }}/runner-cli
      - name: Export runner-cli path
        shell: pwsh
        run: |
          $cliPath = Join-Path $env:RUNNER_TEMP 'runner-cli' 'runner-cli.exe'
          if (Test-Path $cliPath) {
            "LVIE_RUNNER_CLI_PATH=$cliPath" | Out-File -FilePath $Env:GITHUB_ENV -Append -Encoding ascii
            Write-Host ("Using runner-cli at {0}" -f $cliPath)
          } else {
            Write-Warning ("runner-cli artifact missing at {0}" -f $cliPath)
            if ($env:LVIE_REQUIRE_RUNNER_CLI -eq '1') {
              throw "LVIE_REQUIRE_RUNNER_CLI=1 but runner-cli was not found."
            }
          }
      - name: Run vi_validate (strict, report-only)
        uses: ./.github/actions/pylavi-validate
        with:
          config_path: Tooling/pylavi/vi-validate.yml
          label: strict
          log_name: pylavi-validate-strict-log
          report_only: 'true'
          absolute_roots: ${{ vars.LVIE_PYLAVI_ABSOLUTE_PATH_ROOTS }}
      - name: Run vi_validate (legacy, report-only)
        uses: ./.github/actions/pylavi-validate
        with:
          config_path: Tooling/pylavi/vi-validate-legacy.yml
          label: legacy
          log_name: pylavi-validate-legacy-log
          report_only: 'true'
          absolute_roots: ${{ vars.LVIE_PYLAVI_ABSOLUTE_PATH_ROOTS }}

  # Stage 2: Baseline hosted/static checks.
  changes:
    name: Detect VIPC changes (LV ${{ needs.version-gate.outputs.raw }}, shared)
    needs: [run-metadata, prerelease-context, version-gate, pylavi-validate]
    runs-on: ubuntu-latest
    env:
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    outputs:
      vipc: ${{ steps.filter.outputs.vipc }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Runner sanity
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE\\Tooling\\Check-Runner.ps1" -SafeDirectoryScope Global
      - id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            vipc:
              - '**/*.vipc'

  apply-deps-64:
    name: Apply VIPC (LV ${{ needs.version-gate.outputs.raw }} x64)
    needs: [run-metadata, version-gate, changes]
    runs-on: ${{ vars.LVIE_RUNNER_LABEL_64 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Job setup (LV x64)
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          bitness: 64
          create_worktree: 'true'
          worktree_root_mode: runner_temp
      - name: Audit VIPC apply (LV x64)
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $runnerCliArgs = @(
            'vipc', 'assert',
            '--repo-root', $env:REPO_ROOT,
            '--supported-bitness', '64',
            '--vipc-path', '.github/actions/apply-vipc/runner_dependencies.vipc',
            '--output-path', "$env:REPO_ROOT\\builds\\status\\vipc-audit-64.json"
          )
          & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs
      - name: Report VIPC change status (LV x64)
        shell: pwsh
        run: |
          if ('${{ needs.changes.outputs.vipc }}' -eq 'true') {
            Write-Host ".vipc changes detected for this run."
          } else {
            Write-Host "No .vipc changes detected; audit-first policy still enforced."
          }
      - name: Apply VIPC (LV x64, informational)
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.vipc_apply_info == 'true' }}
        continue-on-error: true
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $logPath = Join-Path $env:REPO_ROOT 'builds\status\vipc-apply-64.log'
          $logDir = Split-Path -Path $logPath -Parent
          if (-not (Test-Path -Path $logDir)) {
            New-Item -Path $logDir -ItemType Directory -Force | Out-Null
          }

          '' | Set-Content -Path $logPath -Encoding utf8
          $runnerCliArgs = @(
            'vipc', 'apply',
            '--repo-root', $env:REPO_ROOT,
            '--supported-bitness', '64',
            '--vipc-path', '.github/actions/apply-vipc/runner_dependencies.vipc'
          )
          $output = & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs *>&1
          $exitCode = $LASTEXITCODE

          if ($output) {
            $output |
              ForEach-Object { $_.ToString() } |
              Tee-Object -FilePath $logPath -Append |
              ForEach-Object { Write-Host $_ }
          }

          if ($exitCode -ne 0) {
            exit $exitCode
          }
      - name: Upload VIPC audit artifact (LV x64)
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: vipc-audit-64
          path: ${{ env.REPO_ROOT }}/builds/status/vipc-audit-64.json
          if-no-files-found: warn
      - name: Upload VIPC apply log artifact (LV x64)
        if: ${{ always() && github.event_name == 'workflow_dispatch' && github.event.inputs.vipc_apply_info == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: vipc-apply-log-64
          path: ${{ env.REPO_ROOT }}/builds/status/vipc-apply-64.log
          if-no-files-found: warn
      - name: Job teardown (LV x64)
        if: always()
        uses: ./.github/actions/lvie-job-teardown

  apply-deps-32:
    name: Apply VIPC (LV ${{ needs.version-gate.outputs.raw }} x86)
    needs: [run-metadata, version-gate, changes]
    runs-on: ${{ vars.LVIE_RUNNER_LABEL_32 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Job setup (LV x86)
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          bitness: 32
          create_worktree: 'true'
          worktree_root_mode: runner_temp
      - name: Audit VIPC apply (LV x86)
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $runnerCliArgs = @(
            'vipc', 'assert',
            '--repo-root', $env:REPO_ROOT,
            '--supported-bitness', '32',
            '--vipc-path', '.github/actions/apply-vipc/runner_dependencies.vipc',
            '--output-path', "$env:REPO_ROOT\\builds\\status\\vipc-audit-32.json"
          )
          & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs
      - name: Report VIPC change status (LV x86)
        shell: pwsh
        run: |
          if ('${{ needs.changes.outputs.vipc }}' -eq 'true') {
            Write-Host ".vipc changes detected for this run."
          } else {
            Write-Host "No .vipc changes detected; audit-first policy still enforced."
          }
      - name: Apply VIPC (LV x86, informational)
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.vipc_apply_info == 'true' }}
        continue-on-error: true
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $logPath = Join-Path $env:REPO_ROOT 'builds\status\vipc-apply-32.log'
          $logDir = Split-Path -Path $logPath -Parent
          if (-not (Test-Path -Path $logDir)) {
            New-Item -Path $logDir -ItemType Directory -Force | Out-Null
          }

          '' | Set-Content -Path $logPath -Encoding utf8
          $runnerCliArgs = @(
            'vipc', 'apply',
            '--repo-root', $env:REPO_ROOT,
            '--supported-bitness', '32',
            '--vipc-path', '.github/actions/apply-vipc/runner_dependencies.vipc'
          )
          $output = & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs *>&1
          $exitCode = $LASTEXITCODE

          if ($output) {
            $output |
              ForEach-Object { $_.ToString() } |
              Tee-Object -FilePath $logPath -Append |
              ForEach-Object { Write-Host $_ }
          }

          if ($exitCode -ne 0) {
            exit $exitCode
          }
      - name: Upload VIPC audit artifact (LV x86)
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: vipc-audit-32
          path: ${{ env.REPO_ROOT }}/builds/status/vipc-audit-32.json
          if-no-files-found: warn
      - name: Upload VIPC apply log artifact (LV x86)
        if: ${{ always() && github.event_name == 'workflow_dispatch' && github.event.inputs.vipc_apply_info == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: vipc-apply-log-32
          path: ${{ env.REPO_ROOT }}/builds/status/vipc-apply-32.log
          if-no-files-found: warn
      - name: Job teardown (LV x86)
        if: always()
        uses: ./.github/actions/lvie-job-teardown

  vi-analyzer:
    name: vi-analyzer-${{ matrix.bitness }}-bit
    if: ${{ needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
    needs: [run-metadata, prerelease-context, version-gate, apply-deps-64, apply-deps-32]
    runs-on: ${{ matrix.runner_label }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - bitness: '64'
            runner_label: ${{ vars.LVIE_RUNNER_LABEL_64 || format('self-hosted-windows-lv{0}x64', needs.version-gate.outputs.year) }}
          - bitness: '32'
            runner_label: ${{ vars.LVIE_RUNNER_LABEL_32 || format('self-hosted-windows-lv{0}x86', needs.version-gate.outputs.year) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Job setup
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          bitness: ${{ matrix.bitness }}
          create_worktree: 'true'
          worktree_root_mode: runner_temp

      - name: Run VI Analyzer (LV ${{ matrix.bitness }}-bit)
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $reportsRoot = "builds/vi-analyzer/$env:RUNNER_OS-${{ matrix.bitness }}"
          $statusPath = "builds/status/vi-analyzer-summary-$env:RUNNER_OS-${{ matrix.bitness }}.json"
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          & pwsh -NoProfile -File "$env:REPO_ROOT/Tooling/Run-ViAnalyzer.ps1" `
            -RepoRoot $env:REPO_ROOT `
            -LabVIEWVersion $env:LVIE_REQUIRED_LABVIEW_VERSION `
            -SupportedBitness '${{ matrix.bitness }}' `
            -ReportsRoot $reportsRoot `
            -StatusPath $statusPath

      - name: Upload VI Analyzer reports (LV ${{ matrix.bitness }}-bit)
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: vi-analyzer-reports-${{ runner.os }}-${{ matrix.bitness }}-bit
          path: ${{ env.REPO_ROOT }}/builds/vi-analyzer/${{ runner.os }}-${{ matrix.bitness }}
          if-no-files-found: warn

      - name: Upload VI Analyzer status (LV ${{ matrix.bitness }}-bit)
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: vi-analyzer-status-${{ runner.os }}-${{ matrix.bitness }}-bit
          path: ${{ env.REPO_ROOT }}/builds/status/vi-analyzer-summary-${{ runner.os }}-${{ matrix.bitness }}.json
          if-no-files-found: warn

      - name: Job teardown
        if: always()
        uses: ./.github/actions/lvie-job-teardown

  verify-iepaths:
    name: verify-iepaths-${{ matrix.bitness }}-bit
    if: ${{ needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
    needs: [run-metadata, prerelease-context, version-gate, apply-deps-64, apply-deps-32]
    runs-on: ${{ matrix.runner_label }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - bitness: '64'
            runner_label: ${{ vars.LVIE_RUNNER_LABEL_64 || format('self-hosted-windows-lv{0}x64', needs.version-gate.outputs.year) }}
          - bitness: '32'
            runner_label: ${{ vars.LVIE_RUNNER_LABEL_32 || format('self-hosted-windows-lv{0}x86', needs.version-gate.outputs.year) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: job_setup
        name: Job setup
        continue-on-error: ${{ env.LVIE_VERIFY_IEPATHS_ENFORCE != '1' && env.LVIE_VERIFY_IEPATHS_ENFORCE != 'true' && env.LVIE_VERIFY_IEPATHS_ENFORCE != 'True' }}
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          acquire_lock: 'true'
          bitness: ${{ matrix.bitness }}
          create_worktree: 'true'
          worktree_root_mode: runner_temp

      - name: Close stale LabVIEW before verify-iepaths
        if: ${{ always() && steps.job_setup.outcome == 'success' }}
        continue-on-error: ${{ env.LVIE_VERIFY_IEPATHS_ENFORCE != '1' && env.LVIE_VERIFY_IEPATHS_ENFORCE != 'true' && env.LVIE_VERIFY_IEPATHS_ENFORCE != 'True' }}
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          $repoRoot = if (-not [string]::IsNullOrWhiteSpace($env:REPO_ROOT) -and (Test-Path -LiteralPath $env:REPO_ROOT -PathType Container)) {
            (Resolve-Path -Path $env:REPO_ROOT -ErrorAction Stop).Path
          } else {
            (Resolve-Path -Path $env:GITHUB_WORKSPACE -ErrorAction Stop).Path
          }

          $closeScriptPath = Join-Path $repoRoot '.github\actions\close-labview\Close_LabVIEW.ps1'
          if (-not (Test-Path -LiteralPath $closeScriptPath -PathType Leaf)) {
            throw "Close_LabVIEW.ps1 not found at $closeScriptPath"
          }

          & $closeScriptPath `
            -LabVIEWVersion $env:LVIE_REQUIRED_LABVIEW_VERSION `
            -SupportedBitness '${{ matrix.bitness }}' `
            -TimeoutSeconds 180

          if ($LASTEXITCODE -ne 0) {
            throw ("Close_LabVIEW.ps1 failed with exit code {0}." -f $LASTEXITCODE)
          }

      - id: verify_iepaths
        name: Run verify-iepaths canary
        if: ${{ always() }}
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          function Convert-ToBool {
            param(
              [Parameter(Mandatory = $true)]
              [string]$Value
            )

            return $Value.Trim().ToLowerInvariant() -in @('1', 'true', 'yes', 'on')
          }

          $enforceRaw = if ([string]::IsNullOrWhiteSpace($env:LVIE_VERIFY_IEPATHS_ENFORCE)) {
            '0'
          } else {
            $env:LVIE_VERIFY_IEPATHS_ENFORCE
          }
          $enforceMode = Convert-ToBool -Value $enforceRaw
          $setupOutcome = '${{ steps.job_setup.outcome }}'
          if ([string]::IsNullOrWhiteSpace($setupOutcome)) {
            $setupOutcome = 'unknown'
          }

          $resolvedRepoRoot = if (-not [string]::IsNullOrWhiteSpace($env:REPO_ROOT) -and (Test-Path -LiteralPath $env:REPO_ROOT -PathType Container)) {
            (Resolve-Path -Path $env:REPO_ROOT).Path
          } else {
            (Resolve-Path -Path $env:GITHUB_WORKSPACE).Path
          }

          $runnerOs = if ([string]::IsNullOrWhiteSpace($env:RUNNER_OS)) { 'Unknown' } else { $env:RUNNER_OS }
          $bitness = '${{ matrix.bitness }}'
          $statusArchiveDir = Join-Path $resolvedRepoRoot ("builds\status\verify-iepaths\{0}-{1}" -f $runnerOs, $bitness)
          $evidencePath = Join-Path $resolvedRepoRoot ("builds\status\verify-iepaths-evidence-{0}-{1}.json" -f $runnerOs, $bitness)
          $statusArchiveParent = Split-Path -Path $statusArchiveDir -Parent
          $evidenceParent = Split-Path -Path $evidencePath -Parent
          if (-not (Test-Path -Path $statusArchiveParent -PathType Container)) {
            New-Item -Path $statusArchiveParent -ItemType Directory -Force | Out-Null
          }
          if (-not (Test-Path -Path $statusArchiveDir -PathType Container)) {
            New-Item -Path $statusArchiveDir -ItemType Directory -Force | Out-Null
          }
          if (-not (Test-Path -Path $evidenceParent -PathType Container)) {
            New-Item -Path $evidenceParent -ItemType Directory -Force | Out-Null
          }

          if (-not [string]::IsNullOrWhiteSpace($env:GITHUB_ENV)) {
            "VERIFY_IEPATHS_REPO_ROOT=$resolvedRepoRoot" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
            "VERIFY_IEPATHS_STATUS_DIR=$statusArchiveDir" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
            "VERIFY_IEPATHS_EVIDENCE_PATH=$evidencePath" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          }

          $classification = New-Object System.Collections.Generic.List[string]
          if ($setupOutcome -ne 'success') {
            $classification.Add('setup_failed') | Out-Null
          }

          $scriptExitCode = $null
          $errorMessage = ''
          $verdict = 'pass'

          try {
            $verifyScriptPath = Join-Path $resolvedRepoRoot 'Tooling\Invoke-MissingIEFilesFromLVInstall.ps1'
            if (-not (Test-Path -Path $verifyScriptPath -PathType Leaf)) {
              throw "verify-iepaths script not found at $verifyScriptPath"
            }

            if ([string]::IsNullOrWhiteSpace($env:LVIE_REQUIRED_LABVIEW_VERSION)) {
              throw 'LVIE_REQUIRED_LABVIEW_VERSION was empty; version-gate output is required.'
            }

            $verifyArgs = @(
              '-NoProfile',
              '-File', $verifyScriptPath,
              '-RepoRoot', $resolvedRepoRoot,
              '-LabVIEWVersion', $env:LVIE_REQUIRED_LABVIEW_VERSION,
              '-SupportedBitness', $bitness,
              '-StatusFileArchiveDirectory', $statusArchiveDir,
              '-IgnoreGcliExitCode',
              '-IgnoreStatusFailure'
            )

            $verifyOutput = & pwsh @verifyArgs 2>&1
            $scriptExitCode = if ($null -eq $LASTEXITCODE) { 0 } else { [int]$LASTEXITCODE }
            foreach ($line in @($verifyOutput | ForEach-Object { $_.ToString() })) {
              Write-Host $line
            }
          } catch {
            $errorMessage = $_.Exception.Message
            $scriptExitCode = 1
          } finally {
            if ($null -eq $scriptExitCode) {
              $scriptExitCode = 1
            }
          }

          if ($scriptExitCode -ne 0) {
            $classification.Add('verify_script_nonzero') | Out-Null
            $verdict = 'fail'
          }

          $statusFilePaths = @()
          $missingPathSet = New-Object 'System.Collections.Generic.HashSet[string]' ([System.StringComparer]::OrdinalIgnoreCase)
          if (Test-Path -Path $statusArchiveDir -PathType Container) {
            $statusFiles = @(Get-ChildItem -Path $statusArchiveDir -Filter '*.txt' -File -ErrorAction SilentlyContinue | Sort-Object -Property FullName)
            foreach ($statusFile in $statusFiles) {
              $statusFilePaths += $statusFile.FullName
              $statusRaw = (Get-Content -Path $statusFile.FullName -Raw -ErrorAction SilentlyContinue).Trim()
              if (-not [string]::IsNullOrWhiteSpace($statusRaw)) {
                foreach ($entry in ($statusRaw -split '\s*,\s*')) {
                  if (-not [string]::IsNullOrWhiteSpace($entry)) {
                    [void]$missingPathSet.Add($entry.Trim())
                  }
                }
              }
            }
          }

          $missingPaths = @($missingPathSet)
          if ($missingPaths.Count -gt 0) {
            $classification.Add('missing_paths_detected') | Out-Null
            $verdict = 'fail'
          }

          if (-not [string]::IsNullOrWhiteSpace($errorMessage)) {
            $classification.Add('verify_script_exception') | Out-Null
            $verdict = 'fail'
          }

          $classificationValues = @($classification | Select-Object -Unique)
          if ($classificationValues.Count -eq 0) {
            $classificationValues = @('none')
          }

          if (-not [string]::IsNullOrWhiteSpace($env:GITHUB_STEP_SUMMARY)) {
            Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value ("- Verify IE Paths (LV {0}-bit): enforce={1}; setup={2}; verdict={3}; classifications={4}; missing_paths={5}" -f $bitness, $enforceMode, $setupOutcome, $verdict, ($classificationValues -join ','), $missingPaths.Count)
          }

          $evidence = [ordered]@{
            timestamp_utc = (Get-Date).ToUniversalTime().ToString('o')
            bitness = $bitness
            repo_root = $resolvedRepoRoot
            lvversion_raw = $env:LVIE_REQUIRED_LABVIEW_VERSION
            enforce_mode = $enforceMode
            setup_outcome = $setupOutcome
            script_exit_code = $scriptExitCode
            verdict = $verdict
            classification = $classificationValues -join ','
            status_archive_dir = $statusArchiveDir
            status_files = @($statusFilePaths)
            missing_paths = @($missingPaths)
            error = $errorMessage
          }
          $evidence | ConvertTo-Json -Depth 8 | Set-Content -Path $evidencePath -Encoding utf8
          Write-Host ("verify-iepaths evidence written: {0}" -f $evidencePath)

          if (-not [string]::IsNullOrWhiteSpace($env:GITHUB_OUTPUT)) {
            "verify_iepaths_verdict=$verdict" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "verify_iepaths_classification=$($classificationValues -join ',')" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          }

          if ($verdict -eq 'fail') {
            $message = "verify-iepaths detected issues (setup=$setupOutcome; classifications=$($classificationValues -join ','))."
            if ($enforceMode) {
              throw $message
            }
            Write-Warning ("{0} Canary mode active; continuing with warning." -f $message)
          }

          # Prevent nested pwsh non-zero exit codes from failing canary mode.
          if (-not $enforceMode) {
            $global:LASTEXITCODE = 0
          }

      - name: Upload verify-iepaths status archive (LV ${{ matrix.bitness }}-bit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: verify-iepaths-status-${{ runner.os }}-${{ matrix.bitness }}-bit
          path: ${{ env.VERIFY_IEPATHS_STATUS_DIR }}
          if-no-files-found: warn

      - name: Upload verify-iepaths evidence (LV ${{ matrix.bitness }}-bit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: verify-iepaths-evidence-${{ runner.os }}-${{ matrix.bitness }}-bit
          path: ${{ env.VERIFY_IEPATHS_EVIDENCE_PATH }}
          if-no-files-found: warn

      - name: Job teardown
        if: always()
        uses: ./.github/actions/lvie-job-teardown
        with:
          repo_root: ${{ env.VERIFY_IEPATHS_REPO_ROOT }}
          revert_bitness: ${{ matrix.bitness }}
          close_labview: 'true'
          close_bitness: ${{ matrix.bitness }}
          cleanup_worktree: 'true'

  version:
    name: Compute Version
    needs: [run-metadata, version-gate, prerelease-context]
    runs-on: ${{ vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    outputs:
      VERSION:       ${{ steps.compute.outputs.VERSION }}
      MAJOR:         ${{ steps.compute.outputs.MAJOR }}
      MINOR:         ${{ steps.compute.outputs.MINOR }}
      PATCH:         ${{ steps.compute.outputs.PATCH }}
      BUILD:         ${{ steps.compute.outputs.BUILD }}
      IS_PRERELEASE: ${{ steps.compute.outputs.IS_PRERELEASE }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Job setup
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          create_worktree: 'false'
      - name: Fail on conflicting merged PR release labels
        if: ${{ needs.prerelease-context.outputs.bump_conflict == 'true' }}
        shell: pwsh
        run: |
          $labels = '${{ needs.prerelease-context.outputs.bump_conflict_labels }}'
          $prNumber = '${{ needs.prerelease-context.outputs.merged_pr_number }}'
          if ([string]::IsNullOrWhiteSpace($prNumber)) {
            throw ("Conflicting merged PR release labels detected: {0}" -f $labels)
          }
          throw ("Conflicting merged PR release labels detected on merged PR #{0}: {1}" -f $prNumber, $labels)
      - id: compute
        uses: ./.github/actions/compute-version
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          bump_type_override: ${{ needs.prerelease-context.outputs.bump_type_override }}
      - name: Job teardown
        if: always()
        uses: ./.github/actions/lvie-job-teardown
        with:
          cleanup_worktree: 'false'

  # Stage 3: Self-hosted LabVIEW validation/build chain (profile-gated).
  unit-tests:
    name: Test Source Using LV ${{ matrix.bitness }}-bit
    if: ${{ needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
    needs: [run-metadata, prerelease-context, version-gate, apply-deps-64, apply-deps-32, verify-iepaths, vi-analyzer]
    runs-on: ${{ matrix.runner_label }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
      LVIE_SOURCE_TEST_STRICT: ${{ vars.LVIE_SOURCE_TEST_STRICT || '0' }}
      LVIE_LUNIT_VERBOSE_GCLI: ${{ vars.LVIE_LUNIT_VERBOSE_GCLI || '1' }}
      LVIE_SOURCE_TEST_MODE_INPUT: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.source_test_mode || 'inherit' }}
      LVIE_SOURCE_TEST_LABVIEW_YEAR_OVERRIDE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.source_test_labview_year_override || '' }}
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - bitness: '64'
            runner_label: ${{ vars.LVIE_RUNNER_LABEL_64 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
          - bitness: '32'
            runner_label: ${{ vars.LVIE_RUNNER_LABEL_32 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Job setup
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          bitness: ${{ matrix.bitness }}
          create_worktree: 'true'
          worktree_root_mode: runner_temp
      - name: Assert VIPC dependencies on source-test runner (LV ${{ matrix.bitness }}-bit)
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $precheckOutputPath = Join-Path $env:REPO_ROOT ("builds\\status\\vipc-audit-pretest-{0}-{1}.json" -f $env:RUNNER_OS, '${{ matrix.bitness }}')
          $runnerCliArgs = @(
            'vipc', 'assert',
            '--repo-root', $env:REPO_ROOT,
            '--supported-bitness', '${{ matrix.bitness }}',
            '--vipc-path', '.github/actions/apply-vipc/runner_dependencies.vipc',
            '--output-path', $precheckOutputPath
          )
          & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs
      - name: Test Source Using LV ${{ matrix.bitness }}-bit
        id: source_test
        shell: pwsh
        env:
          LVIE_MATRIX_BITNESS: ${{ matrix.bitness }}
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $isWorkflowDispatch = $env:GITHUB_EVENT_NAME -eq 'workflow_dispatch'
          $unitTestYearFromLvversion = $env:LABVIEW_VERSION_YEAR
          $unitTestYearSourceContract = $unitTestYearFromLvversion
          $unitTestYear = $unitTestYearFromLvversion
          $unitTestLvversionRaw = if ([string]::IsNullOrWhiteSpace($env:LABVIEW_VERSION)) { $env:LVIE_REQUIRED_LABVIEW_VERSION } else { $env:LABVIEW_VERSION }
          $unitTestLvversion = $unitTestLvversionRaw
          $unitTestYearOverrideRaw = if ([string]::IsNullOrWhiteSpace($env:LVIE_SOURCE_TEST_LABVIEW_YEAR_OVERRIDE)) { '' } else { $env:LVIE_SOURCE_TEST_LABVIEW_YEAR_OVERRIDE.Trim() }
          $unitTestYearSource = '.lvversion'
          $yearCompatMappingApplied = $false
          if ([string]::Equals($unitTestYearFromLvversion, '2020', [System.StringComparison]::OrdinalIgnoreCase)) {
            $unitTestYear = '2026'
            $unitTestYearSource = 'lv2020_compat_mapping'
            $yearCompatMappingApplied = $true
          }
          if (-not [string]::IsNullOrWhiteSpace($unitTestYearOverrideRaw)) {
            if (-not $isWorkflowDispatch) {
              throw "source_test_labview_year_override is workflow_dispatch only."
            }
            if ($unitTestYearOverrideRaw -notmatch '^\d{4}$') {
              throw ("Invalid source_test_labview_year_override '{0}'. Expected a four-digit year (for example 2026)." -f $unitTestYearOverrideRaw)
            }
            $unitTestYear = $unitTestYearOverrideRaw
            $unitTestYearSource = 'workflow_dispatch input source_test_labview_year_override'
            $yearCompatMappingApplied = $false
          }
          $sourceTestModeInputRaw = if ([string]::IsNullOrWhiteSpace($env:LVIE_SOURCE_TEST_MODE_INPUT)) { 'inherit' } else { $env:LVIE_SOURCE_TEST_MODE_INPUT.Trim().ToLowerInvariant() }
          if ($sourceTestModeInputRaw -notin @('inherit', 'strict', 'canary')) {
            throw ("Invalid source_test_mode '{0}'. Expected inherit, strict, or canary." -f $sourceTestModeInputRaw)
          }
          if (-not $isWorkflowDispatch -and $sourceTestModeInputRaw -ne 'inherit') {
            throw "source_test_mode override is workflow_dispatch only."
          }
          $strictModeRawFromVariable = if ([string]::IsNullOrWhiteSpace($env:LVIE_SOURCE_TEST_STRICT)) { '0' } else { $env:LVIE_SOURCE_TEST_STRICT.Trim() }
          $strictModeSource = '$env:LVIE_SOURCE_TEST_STRICT'
          $strictModeRaw = $strictModeRawFromVariable
          switch ($sourceTestModeInputRaw) {
            'strict' {
              $strictModeRaw = '1'
              $strictModeSource = 'workflow_dispatch input source_test_mode=strict'
            }
            'canary' {
              $strictModeRaw = '0'
              $strictModeSource = 'workflow_dispatch input source_test_mode=canary'
            }
            default {
              $strictModeRaw = $strictModeRawFromVariable
              $strictModeSource = '$env:LVIE_SOURCE_TEST_STRICT'
            }
          }
          $strictMode = $strictModeRaw.ToLowerInvariant() -notin @('0', 'false', 'no')
          $sourceTestMode = if ($strictMode) { 'strict' } else { 'canary' }
          $yearOverrideApplied = -not [string]::IsNullOrWhiteSpace($unitTestYearOverrideRaw)
          $requestedBitness = if ([string]::IsNullOrWhiteSpace($env:LVIE_MATRIX_BITNESS)) {
            throw "LVIE_MATRIX_BITNESS is required for source-test execution."
          } else {
            $env:LVIE_MATRIX_BITNESS
          }
          Write-Host ("Unit-test LabVIEW target year: {0}" -f $unitTestYear)
          Write-Host ("Unit-test LabVIEW target .lvversion: {0}" -f $unitTestLvversion)
          Write-Host ("Unit-test LabVIEW .lvversion source contract: {0}" -f $unitTestLvversionRaw)
          Write-Host ("Unit-test LabVIEW source-contract year: {0}" -f $unitTestYearSourceContract)
          Write-Host ("Unit-test LabVIEW execution year: {0}" -f $unitTestYear)
          Write-Host ("Unit-test LabVIEW year source: {0}" -f $unitTestYearSource)
          Write-Host ("Unit-test LabVIEW year compat mapping applied: {0}" -f $yearCompatMappingApplied)
          Write-Host ("Unit-test LabVIEW year override applied: {0}" -f $yearOverrideApplied)
          if ($yearCompatMappingApplied) {
            Write-Host "Unit-test LabVIEW year compatibility mapping: source year 2020 -> execution year 2026"
          }
          Write-Host ("Unit-test LabVIEW target bitness: {0}" -f $requestedBitness)
          Write-Host ("Source .lvversion year from job setup: {0}" -f $env:LABVIEW_VERSION_YEAR)
          Write-Host ("Source test mode input: {0}" -f $sourceTestModeInputRaw)
          Write-Host ("Source test mode effective: {0}" -f $sourceTestMode)
          Write-Host ("Source test strict source: {0}" -f $strictModeSource)
          Write-Host ("Source test strict raw value: {0}" -f $strictModeRaw)

          function Resolve-LabVIEWInstallRootForEvidence {
            param(
              [Parameter(Mandatory = $true)][string]$Year,
              [Parameter(Mandatory = $true)][ValidateSet('32','64')][string]$Bitness
            )

            $defaultPath = if ($Bitness -eq '32') {
              "C:\Program Files (x86)\National Instruments\LabVIEW $Year"
            } else {
              "C:\Program Files\National Instruments\LabVIEW $Year"
            }
            if (Test-Path -Path $defaultPath) {
              return $defaultPath
            }

            $registryPath = if ($Bitness -eq '32') {
              "HKLM:\SOFTWARE\WOW6432Node\National Instruments\LabVIEW $Year"
            } else {
              "HKLM:\SOFTWARE\National Instruments\LabVIEW $Year"
            }
            try {
              $props = Get-ItemProperty -Path $registryPath -ErrorAction Stop
              foreach ($name in @('Path', 'InstallDir', 'InstallPath')) {
                $candidate = $props.$name
                if (-not [string]::IsNullOrWhiteSpace($candidate) -and (Test-Path -Path $candidate)) {
                  return $candidate
                }
              }
            } catch {
              return $null
            }

            return $null
          }

          function Invoke-DotnetBuildServerShutdown {
            param([Parameter(Mandatory = $true)][string]$Phase)

            try {
              $shutdownOutput = & dotnet build-server shutdown 2>&1
              $shutdownExitCode = if ($null -eq $LASTEXITCODE) { 0 } else { [int]$LASTEXITCODE }
              foreach ($line in @($shutdownOutput | ForEach-Object { $_.ToString() })) {
                Write-Host ("dotnet build-server shutdown ({0}): {1}" -f $Phase, $line)
              }
              Write-Host ("dotnet build-server shutdown ({0}) exit code: {1}" -f $Phase, $shutdownExitCode)
              return $shutdownExitCode
            } catch {
              Write-Warning ("dotnet build-server shutdown ({0}) failed: {1}" -f $Phase, $_.Exception.Message)
              return 1
            }
          }

          function Save-LabVIEWTempLogs {
            param(
              [Parameter(Mandatory = $true)][DateTime]$SinceUtc,
              [Parameter(Mandatory = $true)][string]$DestinationDir
            )

            $copiedFiles = [System.Collections.Generic.List[string]]::new()
            $tempRoot = [System.IO.Path]::GetTempPath()
            if ([string]::IsNullOrWhiteSpace($tempRoot) -or -not (Test-Path -Path $tempRoot)) {
              Write-Warning ("Temp root unavailable for LabVIEW diagnostics: {0}" -f $tempRoot)
              return $copiedFiles.ToArray()
            }

            $candidates = @(Get-ChildItem -Path $tempRoot -Filter 'lvtemporary_*.log' -File -ErrorAction SilentlyContinue |
              Where-Object { $_.LastWriteTimeUtc -ge $SinceUtc.AddMinutes(-2) } |
              Sort-Object LastWriteTimeUtc -Descending |
              Select-Object -First 20)

            if ($candidates.Count -eq 0) {
              Write-Host ("No run-correlated lvtemporary logs found under {0} after {1:o}" -f $tempRoot, $SinceUtc)
              return $copiedFiles.ToArray()
            }

            if (-not (Test-Path -Path $DestinationDir)) {
              New-Item -Path $DestinationDir -ItemType Directory -Force | Out-Null
            }

            $index = 0
            foreach ($candidate in $candidates) {
              $destinationName = "{0:D2}-{1}" -f $index, $candidate.Name
              $destinationPath = Join-Path $DestinationDir $destinationName
              Copy-Item -Path $candidate.FullName -Destination $destinationPath -Force
              $copiedFiles.Add($destinationPath) | Out-Null
              $index++
            }

            Write-Host ("Saved {0} LabVIEW temp logs to {1}" -f $copiedFiles.Count, $DestinationDir)
            return $copiedFiles.ToArray()
          }

          $installRoot64 = Resolve-LabVIEWInstallRootForEvidence -Year $unitTestYear -Bitness '64'
          $installRoot32 = Resolve-LabVIEWInstallRootForEvidence -Year $unitTestYear -Bitness '32'
          $installDetected64 = -not [string]::IsNullOrWhiteSpace($installRoot64)
          $installDetected32 = -not [string]::IsNullOrWhiteSpace($installRoot32)
          $requestedInstallRoot = if ($requestedBitness -eq '32') { $installRoot32 } else { $installRoot64 }
          $requestedInstallDetected = -not [string]::IsNullOrWhiteSpace($requestedInstallRoot)
          Write-Host ("LabVIEW install evidence for year {0}: 64-bit={1}; 32-bit={2}" -f $unitTestYear, ($installRoot64 ?? '<missing>'), ($installRoot32 ?? '<missing>'))
          Write-Host ("Source test mode: {0} (effective strict raw={1}; source={2})" -f $sourceTestMode, $strictModeRaw, $strictModeSource)
          $verboseGcliRaw = if ([string]::IsNullOrWhiteSpace($env:LVIE_LUNIT_VERBOSE_GCLI)) { '1' } else { $env:LVIE_LUNIT_VERBOSE_GCLI.Trim() }
          $verboseGcli = $verboseGcliRaw.ToLowerInvariant() -notin @('0', 'false', 'no')
          Write-Host ("LUnit verbose g-cli mode: {0} (LVIE_LUNIT_VERBOSE_GCLI={1})" -f $verboseGcli, $verboseGcliRaw)

          $reportOs = if ([string]::IsNullOrWhiteSpace($env:RUNNER_OS)) { 'Unknown' } else { $env:RUNNER_OS }
          $reportPath = Join-Path $env:REPO_ROOT (".github/actions/run-unit-tests/UnitTestReport-{0}-{1}.xml" -f $reportOs, $requestedBitness)
          $legacyReportPath = Join-Path $env:REPO_ROOT '.github/actions/run-unit-tests/UnitTestReport.xml'
          if (Test-Path -Path $reportPath) {
            Remove-Item -Path $reportPath -Force -ErrorAction SilentlyContinue
          }
          if (Test-Path -Path $legacyReportPath) {
            Remove-Item -Path $legacyReportPath -Force -ErrorAction SilentlyContinue
          }
          $lunitLogPath = Join-Path $env:REPO_ROOT ("builds\\status\\lunit-run-{0}-{1}.log" -f $reportOs, $requestedBitness)
          $lunitLogDir = Split-Path -Path $lunitLogPath -Parent
          if (-not (Test-Path -Path $lunitLogDir)) {
            New-Item -Path $lunitLogDir -ItemType Directory -Force | Out-Null
          }
          '' | Set-Content -Path $lunitLogPath -Encoding utf8
          $labviewTempLogDir = Join-Path $env:REPO_ROOT ("builds\\status\\labview-temp-logs-{0}-{1}" -f $reportOs, $requestedBitness)
          if (Test-Path -Path $labviewTempLogDir) {
            Remove-Item -Path $labviewTempLogDir -Recurse -Force -ErrorAction SilentlyContinue
          }

          $closeScriptPath = Join-Path $env:REPO_ROOT '.github/actions/close-labview/Close_LabVIEW.ps1'
          $preCloseExitCode = $null
          if (Test-Path -Path $closeScriptPath -PathType Leaf) {
            try {
              $closeOutput = & pwsh -NoProfile -File $closeScriptPath -LabVIEWVersion $unitTestYear -SupportedBitness $requestedBitness -TimeoutSeconds 120 2>&1
              $preCloseExitCode = if ($null -eq $LASTEXITCODE) { 0 } else { [int]$LASTEXITCODE }
              foreach ($line in @($closeOutput | ForEach-Object { $_.ToString() })) {
                Write-Host $line
              }
              if ($preCloseExitCode -ne 0) {
                Write-Warning ("Pre-run LabVIEW close returned nonzero exit code: {0}" -f $preCloseExitCode)
              }
            } catch {
              $preCloseExitCode = 1
              Write-Warning ("Pre-run LabVIEW close failed: {0}" -f $_.Exception.Message)
            }
          } else {
            Write-Warning ("Close_LabVIEW script not found at {0}" -f $closeScriptPath)
          }

          $buildServerShutdownBeforeExitCode = Invoke-DotnetBuildServerShutdown -Phase 'pre-lunit'

          $runnerCliArgs = @(
            'lunit', 'run',
            '--repo-root', $env:REPO_ROOT,
            '--year', $unitTestYear,
            '--labview-version', $unitTestLvversion,
            '--bitness', $requestedBitness,
            '--project-path', $env:PROJECT_PATH,
            '--report-path', $reportPath
          )
          if ($verboseGcli) {
            $runnerCliArgs += '--verbose-gcli'
          }
          $lunitStartUtc = (Get-Date).ToUniversalTime()
          $output = & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs *>&1
          $runnerCliExitCode = if ($null -eq $LASTEXITCODE) { 0 } else { [int]$LASTEXITCODE }
          $buildServerShutdownAfterExitCode = Invoke-DotnetBuildServerShutdown -Phase 'post-lunit'
          if ($output) {
            $output |
              ForEach-Object { $_.ToString() } |
              Tee-Object -FilePath $lunitLogPath -Append |
              ForEach-Object { Write-Host $_ }
          }
          $gcliExitCode = $null
          $parserExitCode = $null
          $reportPathUsed = $reportPath
          $reportParseSource = 'canonical'
          $reportFallbackUsed = $false
          $classFallbackRecovered = $false
          foreach ($line in @($output | ForEach-Object { $_.ToString() })) {
            if ($line -match 'g-cli lunit exit code:\s*(-?\d+)') {
              $gcliExitCode = [int]$Matches[1]
            } elseif ($line -match 'RunUnitTests parser exit code:\s*(-?\d+)') {
              $parserExitCode = [int]$Matches[1]
            } elseif ($line -match 'Unit test report parse source:\s*(canonical|legacy)\s*$') {
              $reportParseSource = $Matches[1].ToLowerInvariant()
              $reportFallbackUsed = $reportParseSource -eq 'legacy'
            } elseif ($line -match 'Unit test report parse path:\s*(.+)$') {
              $reportPathUsed = $Matches[1].Trim()
            } elseif ($line -match '^LUnit class fallback succeeded with \d+ testcase\(s\) merged into:\s*.+$') {
              $classFallbackRecovered = $true
            }
          }
          if ([string]::IsNullOrWhiteSpace($reportPathUsed)) {
            $reportPathUsed = $reportPath
          }
          Write-Host ("Unit test report parse source resolved by workflow: {0}" -f $reportParseSource)
          Write-Host ("Unit test report path resolved by workflow: {0}" -f $reportPathUsed)
          $labviewTempLogs = @()
          if ($null -ne $gcliExitCode -and $gcliExitCode -ne 0) {
            $labviewTempLogs = Save-LabVIEWTempLogs -SinceUtc $lunitStartUtc -DestinationDir $labviewTempLogDir
          }

          $reportPathCanonical = $reportPath
          $reportExistsCanonical = Test-Path -Path $reportPathCanonical
          $reportExists = Test-Path -Path $reportPathUsed
          if (-not $reportExists -and (Test-Path -Path $legacyReportPath)) {
            Write-Warning ("Canonical unit test report missing at {0}; using legacy alias for workflow parsing: {1}" -f $reportPathUsed, $legacyReportPath)
            $reportPathUsed = $legacyReportPath
            $reportParseSource = 'legacy'
            $reportFallbackUsed = $true
            $reportExists = $true
            if (-not $reportExistsCanonical) {
              try {
                Copy-Item -Path $legacyReportPath -Destination $reportPathCanonical -Force
                $reportExistsCanonical = Test-Path -Path $reportPathCanonical
                if ($reportExistsCanonical) {
                  Write-Host ("Canonical report rehydrated from legacy alias: {0}" -f $reportPathCanonical)
                }
              } catch {
                Write-Warning ("Failed to rehydrate canonical report from legacy alias at {0}: {1}" -f $reportPathCanonical, $_.Exception.Message)
              }
            }
          }
          $legacyReportWritten = $false
          if ($reportExists) {
            $reportUsedIsLegacy = [string]::Equals(
              [System.IO.Path]::GetFullPath($reportPathUsed),
              [System.IO.Path]::GetFullPath($legacyReportPath),
              [System.StringComparison]::OrdinalIgnoreCase
            )
            if ($reportUsedIsLegacy) {
              $legacyReportWritten = $true
              Write-Host ("Legacy UnitTestReport compatibility copy already present: {0}" -f $legacyReportPath)
            } else {
              try {
                Copy-Item -Path $reportPathUsed -Destination $legacyReportPath -Force
                $legacyReportWritten = $true
                Write-Host ("Legacy UnitTestReport compatibility copy written: {0}" -f $legacyReportPath)
              } catch {
                Write-Warning ("Failed to write legacy UnitTestReport compatibility copy at {0}: {1}" -f $legacyReportPath, $_.Exception.Message)
              }
            }
          }
          $testcaseCount = 0
          $reportParseError = $null
          if ($reportExists) {
            try {
              [xml]$reportXml = Get-Content -Path $reportPathUsed -Raw -ErrorAction Stop
              $testCases = $reportXml.SelectNodes('//testcase')
              if ($null -ne $testCases) {
                $testcaseCount = $testCases.Count
              }
            } catch {
              $reportParseError = $_.Exception.Message
            }
          }

          $failureReasons = New-Object System.Collections.Generic.List[string]
          $failureClassifications = New-Object System.Collections.Generic.List[string]
          if (-not $requestedInstallDetected) {
            $failureReasons.Add(("LabVIEW {0} install not detected for requested {1}-bit lane on this runner." -f $unitTestYear, $requestedBitness)) | Out-Null
            $failureClassifications.Add('install_missing_requested_bitness') | Out-Null
          }
          if ($runnerCliExitCode -ne 0) {
            $failureReasons.Add(("runner-cli lunit run exited with code {0}" -f $runnerCliExitCode)) | Out-Null
            $failureClassifications.Add('runner_cli_nonzero') | Out-Null
          }
          if ($null -ne $gcliExitCode -and $gcliExitCode -ne 0) {
            $failureClassifications.Add('gcli_nonzero') | Out-Null
            if ($classFallbackRecovered) {
              $failureClassifications.Add('gcli_nonzero_recovered_by_class_fallback') | Out-Null
              Write-Host ("g-cli primary run was nonzero ({0}) but class fallback recovered testcase evidence." -f $gcliExitCode)
            } else {
              $failureReasons.Add(("g-cli lunit run exited with code {0}" -f $gcliExitCode)) | Out-Null
            }
          }
          if ($null -ne $parserExitCode -and $parserExitCode -ne 0) {
            $failureClassifications.Add('parser_nonzero') | Out-Null
          }
          if (-not $reportExists) {
            $failureReasons.Add("Unit test report missing at $reportPathUsed") | Out-Null
            $failureClassifications.Add('report_missing') | Out-Null
          } elseif (-not [string]::IsNullOrWhiteSpace($reportParseError)) {
            $failureReasons.Add(("Unit test report parse error: {0}" -f $reportParseError)) | Out-Null
            $failureClassifications.Add('report_parse_error') | Out-Null
          } elseif ($testcaseCount -le 0) {
            $failureReasons.Add(("Unit test report has no <testcase> entries: {0}" -f $reportPathUsed)) | Out-Null
            $failureClassifications.Add('empty_testcase_report') | Out-Null
          }
          if (($null -ne $gcliExitCode -and $gcliExitCode -ne 0) -and $reportExists -and $testcaseCount -le 0) {
            $failureClassifications.Add('gcli_nonzero_empty_report') | Out-Null
          }
          if ($null -ne $preCloseExitCode -and $preCloseExitCode -ne 0) {
            $failureClassifications.Add('pre_close_nonzero') | Out-Null
          }
          if ($buildServerShutdownBeforeExitCode -ne 0) {
            $failureClassifications.Add('dotnet_build_server_shutdown_pre_nonzero') | Out-Null
          }
          if ($buildServerShutdownAfterExitCode -ne 0) {
            $failureClassifications.Add('dotnet_build_server_shutdown_post_nonzero') | Out-Null
          }

          $verdict = if ($failureReasons.Count -eq 0) { 'pass' } else { 'fail' }
          $evidencePath = Join-Path $env:REPO_ROOT ("builds\\status\\source-test-evidence-{0}-{1}.json" -f $reportOs, $requestedBitness)
          $evidence = [ordered]@{
            timestamp_utc = (Get-Date).ToUniversalTime().ToString('o')
            lvversion_raw = $unitTestLvversion
            lvversion_source_contract = $unitTestLvversionRaw
            year = $unitTestYear
            year_source_contract = $unitTestYearSourceContract
            year_source = $unitTestYearSource
            year_override_requested = $unitTestYearOverrideRaw
            year_override_applied = $yearOverrideApplied
            year_compat_mapping_applied = $yearCompatMappingApplied
            bitness = $requestedBitness
            install_detected_64 = $installDetected64
            install_detected_32 = $installDetected32
            install_detected_requested_bitness = $requestedInstallDetected
            install_path_64 = if ($installDetected64) { $installRoot64 } else { $null }
            install_path_32 = if ($installDetected32) { $installRoot32 } else { $null }
            report_path = $reportPathUsed
            report_path_canonical = $reportPathCanonical
            report_path_used = $reportPathUsed
            report_fallback_used = $reportFallbackUsed
            report_parse_source = $reportParseSource
            report_exists_canonical = $reportExistsCanonical
            legacy_report_path = $legacyReportPath
            legacy_report_written = $legacyReportWritten
            report_exists = $reportExists
            testcase_count = $testcaseCount
            runner_cli_exit_code = $runnerCliExitCode
            gcli_exit_code = $gcliExitCode
            parser_exit_code = $parserExitCode
            class_fallback_recovered = $classFallbackRecovered
            verbose_gcli_enabled = $verboseGcli
            lunit_log_path = $lunitLogPath
            pre_close_exit_code = $preCloseExitCode
            dotnet_build_server_shutdown_pre_exit_code = $buildServerShutdownBeforeExitCode
            dotnet_build_server_shutdown_post_exit_code = $buildServerShutdownAfterExitCode
            labview_temp_log_dir = $labviewTempLogDir
            labview_temp_logs_collected_count = @($labviewTempLogs).Count
            labview_temp_logs = @($labviewTempLogs)
            failure_classifications = @($failureClassifications)
            strict_mode = $strictMode
            strict_mode_raw = $strictModeRaw
            strict_mode_source = $strictModeSource
            source_test_mode_input = $sourceTestModeInputRaw
            mode = $sourceTestMode
            verdict = $verdict
          }
          $evidence | ConvertTo-Json -Depth 8 | Set-Content -Path $evidencePath -Encoding utf8
          Write-Host ("Source test evidence written: {0}" -f $evidencePath)

          if ($env:GITHUB_STEP_SUMMARY) {
            Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value ("- Source Test Evidence (LV {0}-bit): mode={1}; verdict={2}; testcase_count={3}; runner_cli={4}; gcli={5}; parser={6}; install_detected_requested_bitness={7}; target_year={8}; source_year={9}; lv2020_compat_mapping={10}" -f $requestedBitness, $sourceTestMode, $verdict, $testcaseCount, $runnerCliExitCode, $gcliExitCode, $parserExitCode, $requestedInstallDetected, $unitTestYear, $unitTestYearSourceContract, $yearCompatMappingApplied)
          }
          if ($env:GITHUB_OUTPUT) {
            "source_test_verdict=$verdict" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "report_fallback_used=$([string]$reportFallbackUsed).ToLowerInvariant()" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "source_test_mode_effective=$sourceTestMode" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "source_test_mode_input=$sourceTestModeInputRaw" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "source_test_year=$unitTestYear" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "source_test_year_source_contract=$unitTestYearSourceContract" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "source_test_year_override_applied=$([string]$yearOverrideApplied).ToLowerInvariant()" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            "source_test_year_compat_mapping_applied=$([string]$yearCompatMappingApplied).ToLowerInvariant()" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          }

          if ($failureReasons.Count -gt 0) {
            foreach ($reason in $failureReasons) {
              Write-Warning $reason
            }
            if ($strictMode) {
              $runnerCliExitCode = 1
            } else {
              Write-Warning ("Source-test canary mode active; keeping lane green with evidence verdict '{0}'." -f $verdict)
              $runnerCliExitCode = 0
            }
          }
          Write-Host ("runner-cli lunit run exit code: {0}" -f $runnerCliExitCode)
          exit $runnerCliExitCode
      - name: Summarize unit-test executor
        if: always()
        shell: pwsh
        run: |
          $summaryLine = "- Unit Tests (LV {0}-bit): runner={1}; mode={2}; LUnit executor=runner-cli (g-cli backend); target_year={3}; source_year={4}; lv2020_compat_mapping={5}" -f '${{ matrix.bitness }}', '${{ runner.name }}', '${{ steps.source_test.outputs.source_test_mode_effective }}', '${{ steps.source_test.outputs.source_test_year }}', '${{ steps.source_test.outputs.source_test_year_source_contract }}', '${{ steps.source_test.outputs.source_test_year_compat_mapping_applied }}'
          Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value $summaryLine
      - name: Upload unit test report (LV ${{ matrix.bitness }}-bit)
        if: ${{ failure() || steps.source_test.outputs.source_test_verdict == 'fail' }}
        uses: actions/upload-artifact@v4
        with:
          name: unit-tests-${{ runner.os }}-${{ matrix.bitness }}-bit-report
          path: ${{ env.REPO_ROOT }}/.github/actions/run-unit-tests/UnitTestReport-${{ runner.os }}-${{ matrix.bitness }}.xml
          if-no-files-found: warn
      - name: Upload unit test report legacy alias (LV ${{ matrix.bitness }}-bit)
        if: ${{ failure() || steps.source_test.outputs.source_test_verdict == 'fail' }}
        uses: actions/upload-artifact@v4
        with:
          name: unit-tests-${{ runner.os }}-${{ matrix.bitness }}-bit-report-legacy
          path: ${{ env.REPO_ROOT }}/.github/actions/run-unit-tests/UnitTestReport.xml
          if-no-files-found: warn
      - name: Upload lunit run log (LV ${{ matrix.bitness }}-bit)
        if: ${{ failure() || steps.source_test.outputs.source_test_verdict == 'fail' }}
        uses: actions/upload-artifact@v4
        with:
          name: lunit-run-${{ runner.os }}-${{ matrix.bitness }}-bit-log
          path: ${{ env.REPO_ROOT }}/builds/status/lunit-run-${{ runner.os }}-${{ matrix.bitness }}.log
          if-no-files-found: warn
      - name: Upload LabVIEW temp logs (LV ${{ matrix.bitness }}-bit)
        if: ${{ failure() || steps.source_test.outputs.source_test_verdict == 'fail' }}
        uses: actions/upload-artifact@v4
        with:
          name: labview-temp-logs-${{ runner.os }}-${{ matrix.bitness }}-bit
          path: ${{ env.REPO_ROOT }}/builds/status/labview-temp-logs-${{ runner.os }}-${{ matrix.bitness }}
          if-no-files-found: warn
      - name: Upload source-test evidence (LV ${{ matrix.bitness }}-bit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: source-test-evidence-${{ runner.os }}-${{ matrix.bitness }}-bit
          path: ${{ env.REPO_ROOT }}/builds/status/source-test-evidence-${{ runner.os }}-${{ matrix.bitness }}.json
          if-no-files-found: warn
      - name: Upload VIPC pretest audit (LV ${{ matrix.bitness }}-bit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vipc-audit-pretest-${{ runner.os }}-${{ matrix.bitness }}-bit
          path: ${{ env.REPO_ROOT }}/builds/status/vipc-audit-pretest-${{ runner.os }}-${{ matrix.bitness }}.json
          if-no-files-found: warn
      - name: Job teardown
        if: always()
        uses: ./.github/actions/lvie-job-teardown
        with:
          revert_bitness: ${{ matrix.bitness }}
          close_labview: 'true'
          close_bitness: ${{ matrix.bitness }}

  build-ppl-x86:
    name: Build 32-bit Packed Library
    if: ${{ needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
    needs: [run-metadata, prerelease-context, version-gate, unit-tests, version]
    runs-on: ${{ vars.LVIE_RUNNER_LABEL_32 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Job setup
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          bitness: 32
          create_worktree: 'true'
          worktree_root_mode: runner_temp
      - name: Build PPL (LV 32-bit)
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $runnerCliArgs = @(
            'ppl', 'build',
            '--repo-root', $env:REPO_ROOT,
            '--labview-version', $env:LABVIEW_VERSION_RAW,
            '--supported-bitness', '32',
            '--major', '${{ needs.version.outputs.MAJOR }}',
            '--minor', '${{ needs.version.outputs.MINOR }}',
            '--patch', '${{ needs.version.outputs.PATCH }}',
            '--build', '${{ needs.version.outputs.BUILD }}',
            '--commit', '${{ github.sha }}'
          )
          & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs
          $runnerCliExitCode = if ($null -eq $LASTEXITCODE) { 0 } else { [int]$LASTEXITCODE }
          Write-Host ("runner-cli ppl build exit code: {0}" -f $runnerCliExitCode)
          exit $runnerCliExitCode
      - name: Rename PPL (LV 32-bit)
        shell: pwsh
        run: |
          & "$env:REPO_ROOT\\.github\\actions\\rename-file\\Rename-file.ps1" `
            -CurrentFilename "$env:REPO_ROOT\\resource\\plugins\\lv_icon.lvlibp" `
            -NewFilename "$env:REPO_ROOT\\resource\\plugins\\lv_icon_x86.lvlibp"
      - name: Write packed library report (32-bit)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $reportDir = Join-Path $env:REPO_ROOT 'builds\status'
          New-Item -ItemType Directory -Path $reportDir -Force | Out-Null

          $artifactPath = Join-Path $env:REPO_ROOT 'resource\plugins\lv_icon_x86.lvlibp'
          if (-not (Test-Path -LiteralPath $artifactPath -PathType Leaf)) {
            throw "Expected packed library output was not found: $artifactPath"
          }
          $artifact = Get-Item -LiteralPath $artifactPath

          $reportPath = Join-Path $reportDir ("lv_icon.lvlibp-{0}-32.xml" -f $env:RUNNER_OS)
          $xml = New-Object System.Xml.XmlDocument
          $declaration = $xml.CreateXmlDeclaration('1.0', 'utf-8', $null)
          $null = $xml.AppendChild($declaration)

          $root = $xml.CreateElement('ppl_build_report')
          $root.SetAttribute('schema_version', '1.0')
          $null = $xml.AppendChild($root)

          function Add-ReportField {
            param(
              [Parameter(Mandatory = $true)][string]$Name,
              [Parameter(Mandatory = $true)][string]$Value
            )
            $node = $xml.CreateElement($Name)
            $node.InnerText = $Value
            $null = $root.AppendChild($node)
          }

          Add-ReportField -Name 'artifact_name' -Value $artifact.Name
          Add-ReportField -Name 'artifact_path' -Value $artifact.FullName
          Add-ReportField -Name 'artifact_bytes' -Value ([string]$artifact.Length)
          Add-ReportField -Name 'os' -Value $env:RUNNER_OS
          Add-ReportField -Name 'bitness' -Value '32'
          Add-ReportField -Name 'timestamp_utc' -Value ((Get-Date).ToUniversalTime().ToString('o'))

          $xml.Save($reportPath)
          Write-Host ("Packed library report written: {0}" -f $reportPath)
      - name: Upload packed library artifact (32-bit)
        uses: actions/upload-artifact@v4
        with:
          name: lv_icon_x86.lvlibp
          path: ${{ env.REPO_ROOT }}/resource/plugins/lv_icon_x86.lvlibp
      - name: Upload packed library report artifact (32-bit)
        uses: actions/upload-artifact@v4
        with:
          name: lv_icon.lvlibp-${{ runner.os }}-32-report
          path: ${{ env.REPO_ROOT }}/builds/status/lv_icon.lvlibp-${{ runner.os }}-32.xml
          if-no-files-found: error
      - name: Job teardown
        if: always()
        uses: ./.github/actions/lvie-job-teardown
        with:
          revert_bitness: 32
          close_labview: 'true'
          close_executor: labviewcli
          close_bitness: 32

  build-ppl-x64:
    name: Build 64-bit Packed Library
    if: ${{ needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
    needs: [run-metadata, prerelease-context, version-gate, unit-tests, version]
    runs-on: ${{ vars.LVIE_RUNNER_LABEL_64 || vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Job setup
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          bitness: 64
          create_worktree: 'true'
          worktree_root_mode: runner_temp
      - name: Build PPL (LV 64-bit)
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          $runnerCliProject = Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $runnerCliArgs = @(
            'ppl', 'build',
            '--repo-root', $env:REPO_ROOT,
            '--labview-version', $env:LABVIEW_VERSION_RAW,
            '--supported-bitness', '64',
            '--major', '${{ needs.version.outputs.MAJOR }}',
            '--minor', '${{ needs.version.outputs.MINOR }}',
            '--patch', '${{ needs.version.outputs.PATCH }}',
            '--build', '${{ needs.version.outputs.BUILD }}',
            '--commit', '${{ github.sha }}'
          )
          & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs
          $runnerCliExitCode = if ($null -eq $LASTEXITCODE) { 0 } else { [int]$LASTEXITCODE }
          Write-Host ("runner-cli ppl build exit code: {0}" -f $runnerCliExitCode)
          exit $runnerCliExitCode
      - name: Rename PPL (LV 64-bit)
        shell: pwsh
        run: |
          & "$env:REPO_ROOT\\.github\\actions\\rename-file\\Rename-file.ps1" `
            -CurrentFilename "$env:REPO_ROOT\\resource\\plugins\\lv_icon.lvlibp" `
            -NewFilename "$env:REPO_ROOT\\resource\\plugins\\lv_icon_x64.lvlibp"
      - name: Write packed library report (64-bit)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $reportDir = Join-Path $env:REPO_ROOT 'builds\status'
          New-Item -ItemType Directory -Path $reportDir -Force | Out-Null

          $artifactPath = Join-Path $env:REPO_ROOT 'resource\plugins\lv_icon_x64.lvlibp'
          if (-not (Test-Path -LiteralPath $artifactPath -PathType Leaf)) {
            throw "Expected packed library output was not found: $artifactPath"
          }
          $artifact = Get-Item -LiteralPath $artifactPath

          $reportPath = Join-Path $reportDir ("lv_icon.lvlibp-{0}-64.xml" -f $env:RUNNER_OS)
          $xml = New-Object System.Xml.XmlDocument
          $declaration = $xml.CreateXmlDeclaration('1.0', 'utf-8', $null)
          $null = $xml.AppendChild($declaration)

          $root = $xml.CreateElement('ppl_build_report')
          $root.SetAttribute('schema_version', '1.0')
          $null = $xml.AppendChild($root)

          function Add-ReportField {
            param(
              [Parameter(Mandatory = $true)][string]$Name,
              [Parameter(Mandatory = $true)][string]$Value
            )
            $node = $xml.CreateElement($Name)
            $node.InnerText = $Value
            $null = $root.AppendChild($node)
          }

          Add-ReportField -Name 'artifact_name' -Value $artifact.Name
          Add-ReportField -Name 'artifact_path' -Value $artifact.FullName
          Add-ReportField -Name 'artifact_bytes' -Value ([string]$artifact.Length)
          Add-ReportField -Name 'os' -Value $env:RUNNER_OS
          Add-ReportField -Name 'bitness' -Value '64'
          Add-ReportField -Name 'timestamp_utc' -Value ((Get-Date).ToUniversalTime().ToString('o'))

          $xml.Save($reportPath)
          Write-Host ("Packed library report written: {0}" -f $reportPath)
      - name: Upload packed library artifact (64-bit)
        uses: actions/upload-artifact@v4
        with:
          name: lv_icon_x64.lvlibp
          path: ${{ env.REPO_ROOT }}/resource/plugins/lv_icon_x64.lvlibp
      - name: Upload packed library report artifact (64-bit)
        uses: actions/upload-artifact@v4
        with:
          name: lv_icon.lvlibp-${{ runner.os }}-64-report
          path: ${{ env.REPO_ROOT }}/builds/status/lv_icon.lvlibp-${{ runner.os }}-64.xml
          if-no-files-found: error
      - name: Job teardown
        if: always()
        uses: ./.github/actions/lvie-job-teardown
        with:
          revert_bitness: 64
          close_labview: 'true'
          close_executor: labviewcli
          close_bitness: 64

  build-vip:
    name: Build VI Package
    if: ${{ needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
    needs: [run-metadata, prerelease-context, version-gate, build-ppl-x64, build-ppl-x86, version]
    runs-on: ${{ vars.LVIE_RUNNER_LABEL || 'self-hosted-windows-lv' }}
    outputs:
      vip_status_exists: ${{ steps.vip-status.outputs.exists }}
      vip_artifact_name: ${{ steps.locate-vip.outputs.vip_artifact }}
      vip_file_name: ${{ steps.locate-vip.outputs.vip_name }}
      release_notes_artifact_name: ${{ steps.release-notes.outputs.artifact_name }}
      release_notes_file_name: ${{ steps.release-notes.outputs.asset_name }}
    env:
      LVIE_WORKTREE_ROOT: '${{ github.workspace }}\..\..\lvie\w'
      LVIE_VIPM_TIMEOUT_SECONDS: 900
      LVIE_REQUIRED_LABVIEW_VERSION: ${{ needs.version-gate.outputs.raw }}
      LVIE_REQUIRED_LABVIEW_VERSION_YEAR: ${{ needs.version-gate.outputs.year }}
      LVIE_REQUIRED_LABVIEW_MINOR_REVISION: ${{ needs.version-gate.outputs.minor }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Job setup
        uses: ./.github/actions/lvie-job-setup
        with:
          checkout: 'false'
          bitness: 64
          create_worktree: 'true'
          worktree_root_mode: runner_temp
      - name: Assert VI Package LabVIEW version contract
        shell: pwsh
        run: |
          if ([string]::IsNullOrWhiteSpace($env:LVIE_REQUIRED_LABVIEW_VERSION)) {
            throw "LVIE_REQUIRED_LABVIEW_VERSION is empty. .lvversion-derived version-gate output is required for Build VI Package."
          }
          if ([string]::IsNullOrWhiteSpace($env:LVIE_REQUIRED_LABVIEW_MINOR_REVISION)) {
            throw "LVIE_REQUIRED_LABVIEW_MINOR_REVISION is empty. .lvversion-derived version-gate output is required for Build VI Package."
          }

          if (-not [string]::IsNullOrWhiteSpace($env:LABVIEW_VERSION_RAW) -and $env:LABVIEW_VERSION_RAW -ne $env:LVIE_REQUIRED_LABVIEW_VERSION) {
            throw ("LabVIEW version drift detected. LABVIEW_VERSION_RAW='{0}' does not match version-gate raw '{1}' from .lvversion." -f $env:LABVIEW_VERSION_RAW, $env:LVIE_REQUIRED_LABVIEW_VERSION)
          }
          if (-not [string]::IsNullOrWhiteSpace($env:LABVIEW_MINOR_REVISION) -and $env:LABVIEW_MINOR_REVISION -ne $env:LVIE_REQUIRED_LABVIEW_MINOR_REVISION) {
            throw ("LabVIEW minor revision drift detected. LABVIEW_MINOR_REVISION='{0}' does not match version-gate minor '{1}' from .lvversion." -f $env:LABVIEW_MINOR_REVISION, $env:LVIE_REQUIRED_LABVIEW_MINOR_REVISION)
          }

          Write-Host (".lvversion release contract enforced for Build VI Package: raw={0}, year={1}, minor={2}" -f `
            $env:LVIE_REQUIRED_LABVIEW_VERSION, `
            $env:LVIE_REQUIRED_LABVIEW_VERSION_YEAR, `
            $env:LVIE_REQUIRED_LABVIEW_MINOR_REVISION)
      - name: Generate release notes
        shell: pwsh
        working-directory: ${{ env.REPO_ROOT }}
        run: |
          & ".github/actions/generate-release-notes/GenerateReleaseNotes.ps1" -OutputPath "Tooling/deployment/release_notes.md"
      - uses: actions/download-artifact@v4
        with:
          name: lv_icon_x86.lvlibp
          path: ${{ env.REPO_ROOT }}/resource/plugins
      - uses: actions/download-artifact@v4
        with:
          name: lv_icon_x64.lvlibp
          path: ${{ env.REPO_ROOT }}/resource/plugins
      - name: Generate display information JSON
        id: display-info
        shell: pwsh
        env:
          RELEASE_NOTES_PATH: ${{ env.REPO_ROOT }}/Tooling/deployment/release_notes.md
          REPO_NAME: ${{ github.event.repository.name }}
          REPO_DESCRIPTION: ${{ github.event.repository.description }}
          REPO_URL: ${{ github.event.repository.html_url }}
          COMPANY: ${{ github.repository_owner }}
          REPOSITORY: ${{ github.repository }}
        run: |
          $releaseNotes = if (Test-Path $Env:RELEASE_NOTES_PATH) {
            Get-Content -Raw -Path $Env:RELEASE_NOTES_PATH
          } else {
            "Release notes file not generated."
          }

          $productName = if ([string]::IsNullOrWhiteSpace($Env:REPO_NAME)) {
            $Env:REPOSITORY
          } else {
            $Env:REPO_NAME
          }

          $description = if ([string]::IsNullOrWhiteSpace($Env:REPO_DESCRIPTION)) {
            "$($Env:REPO_NAME) VI Package build for $($Env:REPOSITORY)."
          } else {
            $Env:REPO_DESCRIPTION
          }

          $info = @{
            "Package Version" = @{
              "major" = ${{ needs.version.outputs.MAJOR }}
              "minor" = ${{ needs.version.outputs.MINOR }}
              "patch" = ${{ needs.version.outputs.PATCH }}
              "build" = ${{ needs.version.outputs.BUILD }}
            }
            "Product Name" = $productName
            "Company Name" = $Env:COMPANY
            "Author Name (Person or Company)" = $Env:REPOSITORY
            "Product Homepage (URL)" = if ([string]::IsNullOrWhiteSpace($Env:REPO_URL)) {
              "https://github.com/$($Env:REPOSITORY)"
            } else {
              $Env:REPO_URL
            }
            "Legal Copyright" = "? $(Get-Date -Format yyyy) $($Env:COMPANY)"
            "Product Description Summary" = $description
            "Product Description" = $description
            "Release Notes - Change Log" = $releaseNotes
          }

          $json = $info | ConvertTo-Json -Depth 5 -Compress
          $displayInfoDirectory = Join-Path $env:RUNNER_TEMP 'lvie-vip-build'
          New-Item -Path $displayInfoDirectory -ItemType Directory -Force | Out-Null
          $displayInformationJsonPath = Join-Path $displayInfoDirectory 'display-information.json'
          Set-Content -Path $displayInformationJsonPath -Value $json -Encoding utf8
          "display_information_json_path=$displayInformationJsonPath" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
      - name: Modify VIPB display info (LV 64-bit)
        shell: pwsh
        run: |
          $displayInformationJsonPath = '${{ steps.display-info.outputs.display_information_json_path }}'
          if ([string]::IsNullOrWhiteSpace($displayInformationJsonPath) -or -not (Test-Path -Path $displayInformationJsonPath -PathType Leaf)) {
            throw "Display information JSON path output was missing or invalid: '$displayInformationJsonPath'"
          }
          & "$env:REPO_ROOT\\.github\\actions\\modify-vipb-display-info\\ModifyVIPBDisplayInfo.ps1" `
            -SupportedBitness 64 `
            -RepoRoot "$env:REPO_ROOT" `
            -VIPBPath "Tooling/deployment/NI Icon editor.vipb" `
            -LabVIEWVersion $env:LVIE_REQUIRED_LABVIEW_VERSION `
            -LabVIEWMinorRevision $env:LVIE_REQUIRED_LABVIEW_MINOR_REVISION `
            -Major ${{ needs.version.outputs.MAJOR }} `
            -Minor ${{ needs.version.outputs.MINOR }} `
            -Patch ${{ needs.version.outputs.PATCH }} `
            -Build ${{ needs.version.outputs.BUILD }} `
            -Commit "${{ github.sha }}" `
            -ReleaseNotesFile "$env:REPO_ROOT\\Tooling\\deployment\\release_notes.md" `
            -DisplayInformationJsonPath $displayInformationJsonPath
      - name: Build VI Package (LV 64-bit)
        shell: pwsh
        run: |
          $runnerCliPath = $env:LVIE_RUNNER_CLI_PATH
          $ensureRunnerCliScript = @(
            (Join-Path $env:REPO_ROOT 'Tooling/Ensure-RunnerCli.ps1'),
            (Join-Path $env:GITHUB_WORKSPACE 'Tooling/Ensure-RunnerCli.ps1')
          ) | Where-Object { -not [string]::IsNullOrWhiteSpace($_) -and (Test-Path -Path $_ -PathType Leaf) } | Select-Object -First 1
          if ($ensureRunnerCliScript) {
            $ensureResult = & $ensureRunnerCliScript -RepoRoot $env:REPO_ROOT -RunnerCliPath $runnerCliPath -Require
            if ($ensureResult -and -not [string]::IsNullOrWhiteSpace($ensureResult.Path)) {
              $runnerCliPath = $ensureResult.Path
            }
          }
          $usePrebuiltRunnerCli = -not [string]::IsNullOrWhiteSpace($runnerCliPath) -and (Test-Path -Path $runnerCliPath -PathType Leaf)
          $runnerCliProject = @(
            (Join-Path $env:REPO_ROOT 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'),
            (Join-Path $env:GITHUB_WORKSPACE 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj')
          ) | Where-Object { -not [string]::IsNullOrWhiteSpace($_) -and (Test-Path -Path $_ -PathType Leaf) } | Select-Object -First 1
          if ($usePrebuiltRunnerCli) {
            Write-Host ("Using prebuilt runner-cli at {0}" -f $runnerCliPath)
          } elseif (-not $runnerCliProject) {
            throw "runner-cli unavailable: no usable LVIE_RUNNER_CLI_PATH and no runner-cli project found under REPO_ROOT/GITHUB_WORKSPACE."
          }

          $displayInformationJsonPath = '${{ steps.display-info.outputs.display_information_json_path }}'
          if ([string]::IsNullOrWhiteSpace($displayInformationJsonPath) -or -not (Test-Path -Path $displayInformationJsonPath -PathType Leaf)) {
            throw "Display information JSON path output was missing or invalid: '$displayInformationJsonPath'"
          }

          $runnerCliArgs = @(
            'vip', 'build',
            '--repo-root', $env:REPO_ROOT,
            '--supported-bitness', '64',
            '--vipb-path', 'Tooling/deployment/NI Icon editor.vipb',
            '--labview-version', $env:LVIE_REQUIRED_LABVIEW_VERSION,
            '--labview-minor-revision', $env:LVIE_REQUIRED_LABVIEW_MINOR_REVISION,
            '--major', '${{ needs.version.outputs.MAJOR }}',
            '--minor', '${{ needs.version.outputs.MINOR }}',
            '--patch', '${{ needs.version.outputs.PATCH }}',
            '--build', '${{ needs.version.outputs.BUILD }}',
            '--commit', '${{ github.sha }}',
            '--release-notes-file', "$env:REPO_ROOT\\Tooling\\deployment\\release_notes.md",
            '--display-information-json-path', $displayInformationJsonPath,
            '--vipm-timeout-seconds', $env:LVIE_VIPM_TIMEOUT_SECONDS
          )
          if ($usePrebuiltRunnerCli) {
            & $runnerCliPath @runnerCliArgs
          } else {
            & dotnet run --project $runnerCliProject --configuration Release -- @runnerCliArgs
          }
          if ($LASTEXITCODE -ne 0) {
            exit $LASTEXITCODE
          }
      - name: Check VIP build status file
        id: vip-status
        shell: pwsh
        run: |
          $statusRoot = if (-not [string]::IsNullOrWhiteSpace($env:LVIE_ARTIFACT_ROOT)) { $env:LVIE_ARTIFACT_ROOT } else { $env:REPO_ROOT }
          $statusPath = Join-Path $statusRoot 'builds/status/vip-build.json'
          $exists = Test-Path -Path $statusPath
          "exists=$($exists.ToString().ToLowerInvariant())" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
      - name: Upload VIP build status
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vip-build-status
          path: ${{ env.LVIE_ARTIFACT_ROOT || env.REPO_ROOT }}/builds/status/vip-build.json
          if-no-files-found: warn
      - name: Prepare release notes artifact
        id: release-notes
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $source = "$env:REPO_ROOT\\Tooling\\deployment\\release_notes.md"
          if (-not (Test-Path $source)) {
            Write-Error "Release notes file not found at $source."
            exit 1
          }

          $version = "{0}.{1}.{2}.{3}" -f "${{ needs.version.outputs.MAJOR }}", "${{ needs.version.outputs.MINOR }}", "${{ needs.version.outputs.PATCH }}", "${{ needs.version.outputs.BUILD }}"
          $destName = "release_notes_{0}.md" -f $version
          $artifactName = "release_notes_{0}" -f $version
          $destPath = Join-Path -Path (Split-Path -Parent $source) -ChildPath $destName

          Copy-Item -LiteralPath $source -Destination $destPath -Force
          Write-Host ("Release notes artifact prepared at {0}" -f $destPath)

          "artifact_path=$destPath" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "artifact_name=$artifactName" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "asset_name=$destName" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
      - name: Locate built VI package
        id: locate-vip
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $vipDir = Join-Path -Path $env:REPO_ROOT -ChildPath "builds/VI Package"
          if (-not (Test-Path $vipDir)) {
            Write-Error "VI Package directory not found at '$vipDir'."
            exit 1
          }

          $vipFiles = Get-ChildItem -Path $vipDir -Filter *.vip -File -Recurse | Sort-Object LastWriteTime -Descending
          if (-not $vipFiles) {
            Write-Error "No .vip file found under '$vipDir'."
            exit 1
          }

          if ($vipFiles.Count -gt 1) {
            Write-Warning ("Multiple .vip files found; selecting most recent (by LastWriteTime). Files:`n{0}" -f ($vipFiles | ForEach-Object { " - $($_.FullName)" } | Out-String))
          }

          $vip = $vipFiles[0]
          Write-Host ("Selected .vip: {0}" -f $vip.FullName)

          "vip_path=$($vip.FullName)" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "vip_name=$($vip.Name)" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "vip_artifact=$([System.IO.Path]::GetFileNameWithoutExtension($vip.Name))" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
      - name: Inspect VI Package contents (zip paths)
        shell: pwsh
        run: |
          pwsh -NoProfile -File "$env:REPO_ROOT\\Tooling\\Inspect-VipPackage.ps1" `
            -VipPath "${{ steps.locate-vip.outputs.vip_path }}" `
            -WriteSummary
      - name: Upload release notes
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.release-notes.outputs.artifact_name }}
          path: ${{ steps.release-notes.outputs.artifact_path }}
          compression-level: 0
          if-no-files-found: error
      - name: Upload VI Package
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.locate-vip.outputs.vip_artifact }}
          path: ${{ steps.locate-vip.outputs.vip_path }}
          if-no-files-found: error
      - name: Ensure LabVIEWCLI logs evidence exists
        if: always()
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $logsDir = Join-Path $env:REPO_ROOT 'builds/logs'
          New-Item -Path $logsDir -ItemType Directory -Force | Out-Null

          $existingFiles = Get-ChildItem -Path $logsDir -Recurse -File -ErrorAction SilentlyContinue
          if (-not $existingFiles) {
            $placeholderPath = Join-Path $logsDir 'labviewcli-logs-placeholder.txt'
            $runUrl = "{0}/{1}/actions/runs/{2}" -f $env:GITHUB_SERVER_URL, $env:GITHUB_REPOSITORY, $env:GITHUB_RUN_ID
            @(
              'No LabVIEWCLI log files were produced by this run.'
              'This placeholder preserves the prerelease labviewcli-logs asset contract.'
              ("Run URL: {0}" -f $runUrl)
              ("Generated UTC: {0}" -f (Get-Date).ToUniversalTime().ToString('o'))
            ) | Out-File -FilePath $placeholderPath -Encoding utf8
            Write-Host ("Created LabVIEWCLI logs placeholder at {0}" -f $placeholderPath)
          }
      - name: Upload LabVIEWCLI logs (if present)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: labviewcli-logs
          path: ${{ env.REPO_ROOT }}/builds/logs
          if-no-files-found: error
      - name: Validate tag matches computed version (if provided)
        if: startsWith(github.ref, 'refs/tags/') || github.event.inputs.dispatch_tag != ''
        shell: pwsh
        env:
          EXPECTED_TAG: v${{ needs.version.outputs.MAJOR }}.${{ needs.version.outputs.MINOR }}.${{ needs.version.outputs.PATCH }}.${{ needs.version.outputs.BUILD }}
          REF_TAG: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}
          INPUT_TAG: ${{ github.event.inputs.dispatch_tag || '' }}
        run: |
          $ErrorActionPreference = 'Stop'
          $provided = if (-not [string]::IsNullOrWhiteSpace($Env:INPUT_TAG)) { $Env:INPUT_TAG } else { $Env:REF_TAG }
          if ([string]::IsNullOrWhiteSpace($provided)) {
            Write-Host 'No tag provided; skipping validation.'
            exit 0
          }
          if ($provided -ne $Env:EXPECTED_TAG) {
            Write-Error ("Provided tag '{0}' does not match computed tag '{1}'." -f $provided, $Env:EXPECTED_TAG)
          } else {
            Write-Host ("Tag validation ok: {0}" -f $provided)
          }
      - name: Job teardown
        if: always()
        uses: ./.github/actions/lvie-job-teardown
        with:
          close_labview: 'true'
          close_bitness: 64

  build-ppl-linux-container:
    name: Build Linux Container Packed Library
    needs: [run-metadata, prerelease-context, version, version-gate]
    runs-on: ubuntu-latest
    outputs:
      artifact_name: ${{ steps.asset-meta.outputs.artifact_name }}
      asset_name: ${{ steps.asset-meta.outputs.asset_name }}
    steps:
      - id: asset-meta
        name: Resolve Linux packed library artifact names
        shell: bash
        run: |
          set -euo pipefail
          version='${{ needs.version.outputs.VERSION }}'
          if [[ -z "$version" ]]; then
            echo "Version output is required to name Linux packed library asset." >&2
            exit 1
          fi

          asset_name="lv_icon_linux_${version}.lvlibp"
          artifact_name="lv_icon_linux_container"

          echo "asset_name=$asset_name" >> "$GITHUB_OUTPUT"
          echo "artifact_name=$artifact_name" >> "$GITHUB_OUTPUT"

      - name: Skip Linux packed library build when publish intent is disabled
        if: ${{ needs.prerelease-context.outputs.should_publish != 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Skipping Linux packed library build because prerelease publish intent is disabled."
          echo "- Linux packed library build: skipped (prerelease publish disabled)." >> "$GITHUB_STEP_SUMMARY"

      - name: Checkout code
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/checkout@v4

      - name: Pull LabVIEW Linux image
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: bash
        env:
          LV_REQUESTED_RELEASE: ${{ format('{0}q1', needs.version-gate.outputs.year) }}
        run: |
          set -euo pipefail
          requested_release="${LV_REQUESTED_RELEASE}"
          fallback_release="${LVIE_CONTAINER_PARITY_FALLBACK_RELEASE:-2026q1}"

          try_pull_release() {
            local release="$1"
            local image="nationalinstruments/labview:${release}-linux"
            echo "Using image: ${image}"
            if docker pull "${image}"; then
              echo "LABVIEW_LINUX_IMAGE=${image}" >> "$GITHUB_ENV"
              return 0
            fi
            return 1
          }

          if try_pull_release "${requested_release}"; then
            exit 0
          fi

          if [[ "${requested_release}" != "${fallback_release}" ]]; then
            echo "Requested Linux container release '${requested_release}' is unavailable. Falling back to '${fallback_release}'." >&2
            if try_pull_release "${fallback_release}"; then
              exit 0
            fi
          fi

          echo "Unable to pull LabVIEW Linux image for requested release '${requested_release}' or fallback '${fallback_release}'." >&2
          exit 1

      - name: Build Linux packed library via runner-cli parity run
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: pwsh
        run: |
          $runnerCliProject = Join-Path $env:GITHUB_WORKSPACE 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $contextPath = Join-Path $env:RUNNER_TEMP 'parity-context-linux-container.json'
          & dotnet run --project $runnerCliProject --configuration Release -- parity context --repo-root $env:GITHUB_WORKSPACE --output $contextPath
          if ($LASTEXITCODE -ne 0) {
            throw "runner-cli parity context failed with exit code $LASTEXITCODE."
          }

          & dotnet run --project $runnerCliProject --configuration Release -- parity run --mode linux-container --context $contextPath --build-spec
          if ($LASTEXITCODE -ne 0) {
            throw "runner-cli parity run failed with exit code $LASTEXITCODE."
          }

      - id: prepare-linux-ppl
        name: Prepare Linux packed library release asset
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          source_path="${GITHUB_WORKSPACE}/resource/plugins/lv_icon.lvlibp"
          if [[ ! -f "$source_path" ]]; then
            echo "Expected Linux packed library not found: $source_path" >&2
            exit 1
          fi

          asset_name='${{ steps.asset-meta.outputs.asset_name }}'
          if [[ -z "$asset_name" ]]; then
            echo "Linux packed library asset_name output is empty." >&2
            exit 1
          fi

          dest_dir="${RUNNER_TEMP}/prerelease-linux-ppl"
          mkdir -p "$dest_dir"
          dest_path="${dest_dir}/${asset_name}"
          cp "$source_path" "$dest_path"

          bytes="$(wc -c < "$dest_path" | xargs)"
          echo "Prepared Linux packed library release asset: $dest_path ($bytes bytes)"
          echo "asset_path=$dest_path" >> "$GITHUB_OUTPUT"

      - name: Upload Linux packed library artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.asset-meta.outputs.artifact_name }}
          path: ${{ steps.prepare-linux-ppl.outputs.asset_path }}
          if-no-files-found: error

      - name: Upload Linux container logs
        if: ${{ always() && needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: linux-container-ppl-logs
          path: TestResults/container-parity/linux/logs
          if-no-files-found: warn

  build-ppl-windows-container:
    name: Build Windows Container Packed Library
    needs: [run-metadata, prerelease-context, version, version-gate]
    runs-on: windows-latest
    outputs:
      artifact_name: ${{ steps.asset-meta.outputs.artifact_name }}
      asset_name: ${{ steps.asset-meta.outputs.asset_name }}
    steps:
      - id: asset-meta
        name: Resolve Windows packed library artifact names
        shell: pwsh
        run: |
          $version = '${{ needs.version.outputs.VERSION }}'
          if ([string]::IsNullOrWhiteSpace($version)) {
            throw "Version output is required to name Windows packed library asset."
          }

          $assetName = "lv_icon_windows_{0}.lvlibp" -f $version
          $artifactName = 'lv_icon_windows_container'

          "asset_name=$assetName" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "artifact_name=$artifactName" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append

      - name: Skip Windows packed library build when publish intent is disabled
        if: ${{ needs.prerelease-context.outputs.should_publish != 'true' }}
        shell: pwsh
        run: |
          Write-Host "Skipping Windows packed library build because prerelease publish intent is disabled."
          "- Windows packed library build: skipped (prerelease publish disabled)." | Out-File -FilePath $Env:GITHUB_STEP_SUMMARY -Append

      - name: Checkout code
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/checkout@v4

      - name: Pull LabVIEW Windows image
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: pwsh
        run: |
          $requestedRelease = '${{ needs.version-gate.outputs.year }}q1'
          $fallbackRelease = [Environment]::GetEnvironmentVariable('LVIE_CONTAINER_PARITY_FALLBACK_RELEASE')
          if ([string]::IsNullOrWhiteSpace($fallbackRelease)) {
            $fallbackRelease = '2026q1'
          } else {
            $fallbackRelease = $fallbackRelease.Trim()
          }

          function Try-PullWindowsRelease {
            param(
              [Parameter(Mandatory = $true)]
              [string]$Release
            )

            $image = "nationalinstruments/labview:{0}-windows" -f $Release
            Write-Host "Using image: $image"
            docker pull $image
            if ($LASTEXITCODE -eq 0) {
              "LABVIEW_WINDOWS_IMAGE=$image" | Out-File -FilePath $Env:GITHUB_ENV -Append
              return $true
            }

            return $false
          }

          if (Try-PullWindowsRelease -Release $requestedRelease) {
            return
          }

          if (-not [string]::Equals($requestedRelease, $fallbackRelease, [System.StringComparison]::OrdinalIgnoreCase)) {
            Write-Warning ("Requested Windows container release '{0}' is unavailable. Falling back to '{1}'." -f $requestedRelease, $fallbackRelease)
            if (Try-PullWindowsRelease -Release $fallbackRelease) {
              return
            }
          }

          throw ("Unable to pull LabVIEW Windows image for requested release '{0}' or fallback '{1}'." -f $requestedRelease, $fallbackRelease)

      - name: Validate path contract for Windows container shell
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: pwsh
        run: |
          Write-Host 'Windows container parity script runs under powershell.exe (Windows PowerShell 5.1) inside the NI container.'
          pwsh -NoProfile -File "$env:GITHUB_WORKSPACE\Tooling\Test-PathContract.ps1" -WriteSummary

      - name: Build Windows packed library via runner-cli parity run
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: pwsh
        run: |
          $runnerCliProject = Join-Path $env:GITHUB_WORKSPACE 'Tooling/runner-cli/RunnerCli/RunnerCli.csproj'
          if (-not (Test-Path -Path $runnerCliProject -PathType Leaf)) {
            throw "runner-cli project not found at $runnerCliProject"
          }

          $contextPath = Join-Path $env:RUNNER_TEMP 'parity-context-windows-container.json'
          & dotnet run --project $runnerCliProject --configuration Release -- parity context --repo-root $env:GITHUB_WORKSPACE --output $contextPath
          if ($LASTEXITCODE -ne 0) {
            throw "runner-cli parity context failed with exit code $LASTEXITCODE."
          }

          & dotnet run --project $runnerCliProject --configuration Release -- parity run --mode windows-container --context $contextPath --build-spec
          if ($LASTEXITCODE -ne 0) {
            throw "runner-cli parity run failed with exit code $LASTEXITCODE."
          }

      - id: prepare-windows-ppl
        name: Prepare Windows packed library release asset
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: pwsh
        run: |
          $sourcePath = Join-Path $env:GITHUB_WORKSPACE 'resource\plugins\lv_icon.lvlibp'
          if (-not (Test-Path -LiteralPath $sourcePath -PathType Leaf)) {
            throw "Expected Windows packed library not found: $sourcePath"
          }

          $assetName = '${{ steps.asset-meta.outputs.asset_name }}'
          if ([string]::IsNullOrWhiteSpace($assetName)) {
            throw 'Windows packed library asset_name output is empty.'
          }

          $destDir = Join-Path $env:RUNNER_TEMP 'prerelease-windows-ppl'
          New-Item -Path $destDir -ItemType Directory -Force | Out-Null
          $destPath = Join-Path $destDir $assetName
          Copy-Item -LiteralPath $sourcePath -Destination $destPath -Force

          $item = Get-Item -LiteralPath $destPath
          Write-Host ("Prepared Windows packed library release asset: {0} ({1} bytes)" -f $item.FullName, $item.Length)
          "asset_path=$destPath" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append

      - name: Upload Windows packed library artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.asset-meta.outputs.artifact_name }}
          path: ${{ steps.prepare-windows-ppl.outputs.asset_path }}
          if-no-files-found: error

      - name: Upload Windows container logs
        if: ${{ always() && needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-container-ppl-logs
          path: TestResults/container-parity/windows/logs
          if-no-files-found: warn

  codex-skill-layer-asset:
    name: Resolve Codex Skill Layer Asset
    needs: [run-metadata, prerelease-context]
    runs-on: windows-latest
    permissions:
      contents: read
    outputs:
      artifact_name: ${{ steps.meta.outputs.artifact_name }}
      asset_file_name: ${{ steps.meta.outputs.asset_file_name }}
    steps:
      - id: meta
        name: Resolve artifact metadata
        shell: pwsh
        run: |
          "artifact_name=codex-skill-layer" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          "asset_file_name=lvie-codex-skill-layer-installer.exe" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append

      - name: Checkout code
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/checkout@v4

      - id: resolve
        name: Download and validate pinned skill layer
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          $lockPath = Join-Path $env:GITHUB_WORKSPACE 'Tooling/codex-skill-layer.lock.json'
          if (-not (Test-Path -Path $lockPath -PathType Leaf)) {
            throw "Codex skill lock file not found: $lockPath"
          }

          $lock = Get-Content -Raw -Path $lockPath | ConvertFrom-Json
          foreach ($key in @('repo', 'tag', 'asset_name', 'sha256', 'required_files', 'license_spdx')) {
            if (-not ($lock.PSObject.Properties.Name -contains $key)) {
              throw "Codex skill lock file is missing required key '$key'."
            }
          }

          $destDir = Join-Path $env:RUNNER_TEMP 'prerelease-codex-skill-layer'
          New-Item -Path $destDir -ItemType Directory -Force | Out-Null

          gh release download $lock.tag --repo $lock.repo --pattern $lock.asset_name --dir $destDir --clobber
          if ($LASTEXITCODE -ne 0) {
            throw ("Failed to download codex skill layer asset '{0}' from {1} tag {2}." -f $lock.asset_name, $lock.repo, $lock.tag)
          }

          $assetPath = Join-Path $destDir $lock.asset_name
          if (-not (Test-Path -Path $assetPath -PathType Leaf)) {
            throw "Downloaded codex skill layer asset is missing at $assetPath"
          }

          $actualHash = (Get-FileHash -Path $assetPath -Algorithm SHA256).Hash.ToLowerInvariant()
          $expectedHash = ([string]$lock.sha256).ToLowerInvariant()
          if ($actualHash -ne $expectedHash) {
            throw ("Codex skill layer hash mismatch. expected={0} actual={1}" -f $expectedHash, $actualHash)
          }

          $installDir = Join-Path $destDir 'install'
          if (Test-Path -Path $installDir) {
            Remove-Item -Path $installDir -Recurse -Force
          }
          New-Item -Path $installDir -ItemType Directory -Force | Out-Null

          $silentArgs = @('/S')
          if ($lock.PSObject.Properties.Name -contains 'distribution' -and $null -ne $lock.distribution -and $lock.distribution.PSObject.Properties.Name -contains 'silent_args') {
            $candidateSilentArgs = @($lock.distribution.silent_args | ForEach-Object { [string]$_ } | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
            if ($candidateSilentArgs.Count -gt 0) {
              $silentArgs = $candidateSilentArgs
            }
          }
          $arguments = @($silentArgs + @("/D=$installDir"))
          $installProcess = Start-Process -FilePath $assetPath -ArgumentList $arguments -PassThru -Wait
          if ($installProcess.ExitCode -ne 0) {
            throw ("Codex skill layer installer failed with exit code {0}." -f $installProcess.ExitCode)
          }

          $manifestPath = Join-Path $installDir 'manifest.json'
          if (-not (Test-Path -Path $manifestPath -PathType Leaf)) {
            throw "Codex skill layer manifest missing: $manifestPath"
          }

          $manifest = Get-Content -Raw -Path $manifestPath | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace([string]$manifest.license_spdx) -or [string]$manifest.license_spdx -ne [string]$lock.license_spdx) {
            throw ("Codex skill layer license mismatch. expected={0} actual={1}" -f [string]$lock.license_spdx, [string]$manifest.license_spdx)
          }

          foreach ($relative in @($lock.required_files)) {
            $requiredPath = Join-Path $installDir ([string]$relative)
            if (-not (Test-Path -Path $requiredPath)) {
              throw ("Codex skill layer required file missing: {0}" -f $relative)
            }
          }

          "asset_path=$assetPath" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append

      - name: Upload codex skill layer artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.artifact_name }}
          path: ${{ steps.resolve.outputs.asset_path }}
          if-no-files-found: error

  publish-gate:
    name: Publish Gate
    if: always()
    needs:
      - run-metadata
      - prerelease-context
      - version-gate
      - powershell-lint
      - vip-prerelease-requirements-lint
      - docs-lint
      - pylavi-validate
      - changes
      - apply-deps-64
      - apply-deps-32
      - verify-iepaths
      - vi-analyzer
      - unit-tests
      - build-ppl-x64
      - build-ppl-x86
      - build-ppl-linux-container
      - build-ppl-windows-container
      - codex-skill-layer-asset
      - build-vip
      - version
      - conformance-full
    runs-on: ubuntu-latest
    outputs:
      gate_reason: ${{ steps.gate.outputs.gate_reason }}
      ci_profile: ${{ steps.gate.outputs.ci_profile }}
    steps:
      - id: gate
        name: Evaluate profile-required prepublish jobs
        shell: pwsh
        run: |
          $shouldPublish = '${{ needs.prerelease-context.outputs.should_publish }}' -eq 'true'
          $ciProfile = '${{ needs.prerelease-context.outputs.ci_profile }}'
          if ([string]::IsNullOrWhiteSpace($ciProfile)) {
            Write-Warning 'ci_profile output was empty; defaulting to full for prepublish gating.'
            $ciProfile = 'full'
          }

          "ci_profile=$ciProfile" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append

          if (-not $shouldPublish) {
            "gate_reason=publish-intent-disabled" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
            Write-Host 'Publish intent is disabled for this run; prepublish gate passes by policy.'
            exit 0
          }

          $requiredCommon = @(
            'run-metadata',
            'prerelease-context',
            'version-gate',
            'powershell-lint',
            'vip-prerelease-requirements-lint',
            'docs-lint',
            'pylavi-validate',
            'build-ppl-linux-container',
            'build-ppl-windows-container',
            'codex-skill-layer-asset',
            'version',
            'conformance-full'
          )
          $requiredFullValidation = @(
            'changes',
            'apply-deps-64',
            'apply-deps-32',
            'verify-iepaths',
            'vi-analyzer',
            'unit-tests',
            'build-ppl-x64',
            'build-ppl-x86',
            'build-vip'
          )
          $requiredByProfile = @{
            'full' = @($requiredCommon + $requiredFullValidation)
            'pr-fast' = @($requiredCommon + $requiredFullValidation)
            'release-priority' = @($requiredCommon)
          }
          if (-not $requiredByProfile.ContainsKey($ciProfile)) {
            Write-Warning ("Unknown ci_profile '{0}'; defaulting to full prepublish contract." -f $ciProfile)
            $ciProfile = 'full'
            "ci_profile=$ciProfile" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          }

          $required = $requiredByProfile[$ciProfile]
          $needs = ConvertFrom-Json -InputObject '${{ toJson(needs) }}'
          $needsProps = $needs.PSObject.Properties

          $missing = @()
          $failing = @()
          foreach ($job in $required) {
            $entryProp = $needsProps[$job]
            if (-not $entryProp) {
              $missing += "$job:missing"
              continue
            }
            $result = [string]$entryProp.Value.result
            if ($result -ne 'success') {
              $failing += "{0}:{1}" -f $job, $result
            }
          }

          $gatePasses = ($missing.Count -eq 0 -and $failing.Count -eq 0)
          $gateReason = if ($gatePasses) { 'prepublish-required-jobs-satisfied' } else { 'prepublish-required-jobs-failed' }
          "gate_reason=$gateReason" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append

          $missingLabel = if ($missing.Count -gt 0) { $missing -join ', ' } else { '<none>' }
          $failingLabel = if ($failing.Count -gt 0) { $failing -join ', ' } else { '<none>' }
          Write-Host ("Prepublish gate profile: {0}" -f $ciProfile)
          Write-Host ("Prepublish gate reason: {0}" -f $gateReason)
          Write-Host ("Missing required jobs: {0}" -f $missingLabel)
          Write-Host ("Failing/non-success required jobs: {0}" -f $failingLabel)

          if (-not $gatePasses) {
            throw ("Publish gate blocked prerelease publication for profile '{0}': missing={1}; failing={2}" -f $ciProfile, $missingLabel, $failingLabel)
          }

  publish-prerelease:
    name: Publish Pre-Release
    if: ${{ always() && needs.publish-gate.result == 'success' && needs.run-metadata.result == 'success' && needs.prerelease-context.result == 'success' && needs.version.result == 'success' && needs.build-ppl-linux-container.result == 'success' && needs.build-ppl-windows-container.result == 'success' && needs.codex-skill-layer-asset.result == 'success' && (needs.prerelease-context.outputs.ci_profile == 'release-priority' || needs.build-vip.result == 'success') }}
    needs: [run-metadata, prerelease-context, version, build-vip, build-ppl-linux-container, build-ppl-windows-container, codex-skill-layer-asset, publish-gate]
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
    outputs:
      release_tag: ${{ steps.publish.outputs.release_tag }}
      release_url: ${{ steps.publish.outputs.release_url }}
      release_id: ${{ steps.publish.outputs.release_id }}
      publish_status: ${{ steps.publish.outputs.publish_status }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Report publish gate decision
        shell: pwsh
        run: |
          Write-Host ("Publish gate reason: {0}" -f '${{ needs.publish-gate.outputs.gate_reason }}')

      - name: Download VI package artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' && needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build-vip.outputs.vip_artifact_name }}
          path: ${{ runner.temp }}/prerelease-assets/vip

      - name: Download release notes artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' && needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build-vip.outputs.release_notes_artifact_name }}
          path: ${{ runner.temp }}/prerelease-assets/release-notes

      - name: Download LabVIEWCLI logs artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' && needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: labviewcli-logs
          path: ${{ runner.temp }}/prerelease-assets/labviewcli-logs

      - name: Download VIP build status artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' && needs.prerelease-context.outputs.ci_profile != 'release-priority' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: vip-build-status
          path: ${{ runner.temp }}/prerelease-assets/vip-build-status

      - name: Download Linux packed library artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build-ppl-linux-container.outputs.artifact_name }}
          path: ${{ runner.temp }}/prerelease-assets/linux-ppl

      - name: Download Windows packed library artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build-ppl-windows-container.outputs.artifact_name }}
          path: ${{ runner.temp }}/prerelease-assets/windows-ppl

      - name: Download Codex skill layer artifact
        if: ${{ needs.prerelease-context.outputs.should_publish == 'true' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.codex-skill-layer-asset.outputs.artifact_name }}
          path: ${{ runner.temp }}/prerelease-assets/codex-skill-layer

      - id: publish
        name: Publish prerelease and write status
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $ErrorActionPreference = 'Stop'

          $statusDir = Join-Path $env:GITHUB_WORKSPACE 'builds/status'
          New-Item -Path $statusDir -ItemType Directory -Force | Out-Null
          $statusPath = Join-Path $statusDir 'prerelease-publish.json'

          $shouldPublish = '${{ needs.prerelease-context.outputs.should_publish }}' -eq 'true'
          $ciProfile = '${{ needs.prerelease-context.outputs.ci_profile }}'
          $releasePriorityMode = $ciProfile -eq 'release-priority'
          $publishMode = '${{ needs.prerelease-context.outputs.publish_mode }}'
          $publishReason = '${{ needs.prerelease-context.outputs.publish_reason }}'
          $publishGateReason = '${{ needs.publish-gate.outputs.gate_reason }}'
          $mergedPrNumber = '${{ needs.prerelease-context.outputs.merged_pr_number }}'
          $tag = '${{ needs.version.outputs.VERSION }}'
          $expectedVipFileName = '${{ needs.build-vip.outputs.vip_file_name }}'
          $expectedReleaseNotesFileName = '${{ needs.build-vip.outputs.release_notes_file_name }}'
          $expectedLinuxPackedLibraryFileName = '${{ needs.build-ppl-linux-container.outputs.asset_name }}'
          $expectedWindowsPackedLibraryFileName = '${{ needs.build-ppl-windows-container.outputs.asset_name }}'
          $expectedCodexSkillLayerFileName = '${{ needs.codex-skill-layer-asset.outputs.asset_file_name }}'

          $publishStatus = 'skipped'
          $releaseUrl = ''
          $releaseId = ''
          $attachedAssets = @()
          $requiredAssets = @()
          $errorMessage = ''

          try {
            if ($shouldPublish) {
              $assetRoot = Join-Path $env:RUNNER_TEMP 'prerelease-assets'
              if ($releasePriorityMode) {
                $requiredAssets = @('linux-packed-library', 'windows-packed-library', 'codex-skill-layer')
              } else {
                $requiredAssets = @('vip', 'release_notes', 'labviewcli-logs', 'vip-build-status', 'linux-packed-library', 'windows-packed-library', 'codex-skill-layer')
              }
              if (-not $releasePriorityMode -and [string]::IsNullOrWhiteSpace($expectedVipFileName)) {
                throw 'Required build-vip output missing: vip_file_name.'
              }
              if (-not $releasePriorityMode -and [string]::IsNullOrWhiteSpace($expectedReleaseNotesFileName)) {
                throw 'Required build-vip output missing: release_notes_file_name.'
              }
              if ([string]::IsNullOrWhiteSpace($expectedLinuxPackedLibraryFileName)) {
                throw 'Required build-ppl-linux-container output missing: asset_name.'
              }
              if ([string]::IsNullOrWhiteSpace($expectedWindowsPackedLibraryFileName)) {
                throw 'Required build-ppl-windows-container output missing: asset_name.'
              }
              if ([string]::IsNullOrWhiteSpace($expectedCodexSkillLayerFileName)) {
                throw 'Required codex-skill-layer-asset output missing: asset_file_name.'
              }

              $vipFile = $null
              $notesFile = $null
              $vipStatusFile = $null
              $labviewcliZip = $null
              $codexSkillLayerFile = $null

              if (-not $releasePriorityMode) {
                $vipFiles = Get-ChildItem -Path (Join-Path $assetRoot 'vip') -Filter $expectedVipFileName -File -Recurse -ErrorAction SilentlyContinue | Sort-Object FullName
                if (-not $vipFiles) { throw ("Required asset missing: expected VIP file '{0}'." -f $expectedVipFileName) }
                $vipFile = $vipFiles[0]

                $notesFiles = Get-ChildItem -Path (Join-Path $assetRoot 'release-notes') -Filter $expectedReleaseNotesFileName -File -Recurse -ErrorAction SilentlyContinue | Sort-Object FullName
                if (-not $notesFiles) { throw ("Required asset missing: expected release notes file '{0}'." -f $expectedReleaseNotesFileName) }
                $notesFile = $notesFiles[0]

                $vipStatusFile = Get-ChildItem -Path (Join-Path $assetRoot 'vip-build-status') -Filter vip-build.json -File -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
                if (-not $vipStatusFile) { throw 'Required asset missing: vip-build-status/vip-build.json.' }
              }

              $linuxPackedLibraryFiles = Get-ChildItem -Path (Join-Path $assetRoot 'linux-ppl') -Filter $expectedLinuxPackedLibraryFileName -File -Recurse -ErrorAction SilentlyContinue | Sort-Object FullName
              if (-not $linuxPackedLibraryFiles) { throw ("Required asset missing: expected Linux packed library '{0}'." -f $expectedLinuxPackedLibraryFileName) }
              $linuxPackedLibraryFile = $linuxPackedLibraryFiles[0]

              $windowsPackedLibraryFiles = Get-ChildItem -Path (Join-Path $assetRoot 'windows-ppl') -Filter $expectedWindowsPackedLibraryFileName -File -Recurse -ErrorAction SilentlyContinue | Sort-Object FullName
              if (-not $windowsPackedLibraryFiles) { throw ("Required asset missing: expected Windows packed library '{0}'." -f $expectedWindowsPackedLibraryFileName) }
              $windowsPackedLibraryFile = $windowsPackedLibraryFiles[0]

              $codexSkillLayerFiles = Get-ChildItem -Path (Join-Path $assetRoot 'codex-skill-layer') -Filter $expectedCodexSkillLayerFileName -File -Recurse -ErrorAction SilentlyContinue | Sort-Object FullName
              if (-not $codexSkillLayerFiles) { throw ("Required asset missing: expected codex skill layer '{0}'." -f $expectedCodexSkillLayerFileName) }
              $codexSkillLayerFile = $codexSkillLayerFiles[0]

              if (-not $releasePriorityMode) {
                $labviewcliDir = Join-Path $assetRoot 'labviewcli-logs'
                if (-not (Test-Path $labviewcliDir)) { throw 'Required asset missing: labviewcli-logs artifact.' }
                $labviewcliFiles = Get-ChildItem -Path $labviewcliDir -Recurse -File -ErrorAction SilentlyContinue
                if (-not $labviewcliFiles) { throw 'Required asset missing: labviewcli-logs artifact content.' }
                $labviewcliZip = Join-Path $assetRoot 'labviewcli-logs.zip'
                if (Test-Path $labviewcliZip) {
                  Remove-Item -Path $labviewcliZip -Force
                }
                Compress-Archive -Path (Join-Path $labviewcliDir '*') -DestinationPath $labviewcliZip -Force
              }

              $null = gh --version

              $lvversionPath = Join-Path $env:GITHUB_WORKSPACE '.lvversion'
              if (-not (Test-Path -Path $lvversionPath -PathType Leaf)) {
                throw "Cannot build release notes body: .lvversion not found at $lvversionPath"
              }
              $minimumSupportedLabVIEWVersion = (Get-Content -Raw -Path $lvversionPath).Trim()
              if ([string]::IsNullOrWhiteSpace($minimumSupportedLabVIEWVersion)) {
                throw "Cannot build release notes body: .lvversion is empty."
              }
              $minimumVersionLine = "Minimum supported LabVIEW version: $minimumSupportedLabVIEWVersion"

              if ($releasePriorityMode) {
                $releaseBody = @(
                  "# Release Priority Prerelease"
                  ''
                  "This prerelease was published in release-priority mode."
                  $minimumVersionLine
                  ''
                  "Included assets:"
                  "- $($linuxPackedLibraryFile.Name)"
                  "- $($windowsPackedLibraryFile.Name)"
                  "- $($codexSkillLayerFile.Name)"
                ) -join [Environment]::NewLine
              } else {
                $releaseBody = Get-Content -Raw -Path $notesFile.FullName
                if ($releaseBody -notmatch [regex]::Escape($minimumVersionLine)) {
                  $releaseBody = ($releaseBody.TrimEnd() + [Environment]::NewLine + [Environment]::NewLine + $minimumVersionLine + [Environment]::NewLine)
                }
              }
              $payloadPath = Join-Path $assetRoot 'release-payload.json'
              $payload = [ordered]@{
                tag_name = $tag
                target_commitish = $env:GITHUB_SHA
                name = $tag
                body = $releaseBody
                draft = $false
                prerelease = $true
              }
              $payload | ConvertTo-Json -Depth 8 | Out-File -FilePath $payloadPath -Encoding utf8

              $repo = $env:GITHUB_REPOSITORY
              $existing = $null
              try {
                $existingRaw = gh api --method GET -H "Accept: application/vnd.github+json" "/repos/$repo/releases/tags/$tag" 2>$null
                if ($LASTEXITCODE -eq 0 -and -not [string]::IsNullOrWhiteSpace($existingRaw)) {
                  $existing = $existingRaw | ConvertFrom-Json
                }
              } catch {
                $existing = $null
              }

              if ($existing) {
                $release = $existing
                $releaseUrl = [string]$release.html_url
                $releaseId = [string]$release.id

                $expectedAssetNames = @($linuxPackedLibraryFile.Name, $windowsPackedLibraryFile.Name, $codexSkillLayerFile.Name)
                if (-not $releasePriorityMode) {
                  $expectedAssetNames = @($vipFile.Name, $notesFile.Name, (Split-Path -Path $labviewcliZip -Leaf), $vipStatusFile.Name) + $expectedAssetNames
                }

                $existingAssetNames = @()
                foreach ($asset in @($release.assets)) {
                  if ($null -ne $asset -and -not [string]::IsNullOrWhiteSpace([string]$asset.name)) {
                    $existingAssetNames += [string]$asset.name
                  }
                }

                $missingAssets = New-Object System.Collections.Generic.List[string]
                foreach ($assetName in $expectedAssetNames) {
                  if (-not ($existingAssetNames -contains $assetName)) {
                    $missingAssets.Add($assetName) | Out-Null
                  }
                }

                if ($missingAssets.Count -gt 0) {
                  throw ("Immutable release '{0}' already exists but is missing required assets: {1}" -f $tag, ($missingAssets.ToArray() -join ', '))
                }

                $publishStatus = 'already-published'
                $attachedAssets = @($existingAssetNames)
              } else {
                $releaseRaw = gh api --method POST -H "Accept: application/vnd.github+json" "/repos/$repo/releases" --input $payloadPath
                if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($releaseRaw)) {
                  throw "Failed to create release metadata for tag '$tag'."
                }

                $release = $releaseRaw | ConvertFrom-Json
                $releaseUrl = [string]$release.html_url
                $releaseId = [string]$release.id

                $uploadAssets = @($linuxPackedLibraryFile.FullName, $windowsPackedLibraryFile.FullName, $codexSkillLayerFile.FullName)
                if (-not $releasePriorityMode) {
                  $uploadAssets = @($vipFile.FullName, $notesFile.FullName, $labviewcliZip, $vipStatusFile.FullName) + $uploadAssets
                }

                & gh release upload $tag @uploadAssets
                if ($LASTEXITCODE -ne 0) {
                  throw "Failed to upload release assets for tag '$tag'."
                }

                $publishStatus = 'published'
                if ($releasePriorityMode) {
                  $attachedAssets = @($linuxPackedLibraryFile.Name, $windowsPackedLibraryFile.Name, $codexSkillLayerFile.Name)
                } else {
                  $attachedAssets = @($vipFile.Name, $notesFile.Name, (Split-Path -Path $labviewcliZip -Leaf), $vipStatusFile.Name, $linuxPackedLibraryFile.Name, $windowsPackedLibraryFile.Name, $codexSkillLayerFile.Name)
                }
              }
            } else {
              if ($releasePriorityMode) {
                $requiredAssets = @('linux-packed-library', 'windows-packed-library', 'codex-skill-layer')
              } else {
                $requiredAssets = @('vip', 'release_notes', 'labviewcli-logs', 'vip-build-status', 'linux-packed-library', 'windows-packed-library', 'codex-skill-layer')
              }
            }
          } catch {
            $publishStatus = 'failed'
            $errorMessage = $_.Exception.Message
          } finally {
            $status = [ordered]@{
              run_id = '${{ github.run_id }}'
              run_attempt = '${{ github.run_attempt }}'
              sha = '${{ github.sha }}'
              ref = '${{ github.ref }}'
              event = '${{ github.event_name }}'
              publish_mode = $publishMode
              publish_reason = $publishReason
              publish_gate_reason = $publishGateReason
              ci_profile = $ciProfile
              release_priority_mode = $releasePriorityMode
              should_publish = $shouldPublish
              publish_status = $publishStatus
              version = $tag
              release_tag = $tag
              release_url = $releaseUrl
              release_id = $releaseId
              merged_pr_number = $mergedPrNumber
              required_assets = $requiredAssets
              attached_assets = $attachedAssets
              error = $errorMessage
              generated_utc = (Get-Date).ToUniversalTime().ToString('o')
            }
            $status | ConvertTo-Json -Depth 8 | Out-File -FilePath $statusPath -Encoding utf8

            "release_tag=$tag" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
            "release_url=$releaseUrl" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
            "release_id=$releaseId" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
            "publish_status=$publishStatus" | Out-File -FilePath $Env:GITHUB_OUTPUT -Append
          }

          if ($publishStatus -eq 'failed') {
            throw $errorMessage
          }

      - name: Upload prerelease publish status
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: prerelease-publish-status
          path: builds/status/prerelease-publish.json
          if-no-files-found: error

      - name: Upload solo release evidence (if present)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: solo-release-evidence
          path: builds/status/solo-release-evidence.json
          if-no-files-found: warn

  pipeline-contract:
    name: Pipeline Contract
    if: always()
    needs:
      - run-metadata
      - prerelease-context
      - version-gate
      - powershell-lint
      - vip-prerelease-requirements-lint
      - docs-lint
      - pylavi-validate
      - changes
      - apply-deps-64
      - apply-deps-32
      - verify-iepaths
      - vi-analyzer
      - unit-tests
      - build-ppl-x64
      - build-ppl-x86
      - build-ppl-linux-container
      - build-ppl-windows-container
      - codex-skill-layer-asset
      - build-vip
      - publish-gate
      - publish-prerelease
      - version
      - conformance-full
    runs-on: ubuntu-latest
    steps:
      - name: Download VIP build status (if available)
        if: ${{ needs.build-vip.outputs.vip_status_exists == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: vip-build-status
          path: vip-build-status
      - name: Validate required jobs
        shell: pwsh
        run: |
          $shouldPublish = '${{ needs.prerelease-context.outputs.should_publish }}' -eq 'true'
          $requiredCommon = @(
            'run-metadata',
            'prerelease-context',
            'version-gate',
            'powershell-lint',
            'vip-prerelease-requirements-lint',
            'docs-lint',
            'pylavi-validate',
            'build-ppl-linux-container',
            'build-ppl-windows-container',
            'codex-skill-layer-asset',
            'publish-gate',
            'version',
            'conformance-full'
          )
          if ($shouldPublish) {
            $requiredCommon += @('publish-prerelease')
          } else {
            Write-Host 'Publish intent disabled: publish-prerelease is optional in pipeline contract.'
          }
          $requiredFullValidation = @(
            'changes',
            'apply-deps-64',
            'apply-deps-32',
            'verify-iepaths',
            'vi-analyzer',
            'unit-tests',
            'build-ppl-x64',
            'build-ppl-x86',
            'build-vip'
          )
          $requiredByProfile = @{
            'full' = @($requiredCommon + $requiredFullValidation)
            'pr-fast' = @($requiredCommon + $requiredFullValidation)
            'release-priority' = @($requiredCommon)
          }

          $ciProfile = '${{ needs.prerelease-context.outputs.ci_profile }}'
          if ([string]::IsNullOrWhiteSpace($ciProfile)) {
            Write-Warning 'ci_profile output was empty; defaulting to full profile contract.'
            $ciProfile = 'full'
          }
          if (-not $requiredByProfile.ContainsKey($ciProfile)) {
            Write-Warning ("Unknown ci_profile '{0}'; defaulting to full profile contract." -f $ciProfile)
            $ciProfile = 'full'
          }

          $required = $requiredByProfile[$ciProfile]
          Write-Host ("CI profile: {0}" -f $ciProfile)
          Write-Host ("Required jobs ({0}): {1}" -f $required.Count, ($required -join ', '))

          $needs = ConvertFrom-Json -InputObject '${{ toJson(needs) }}'
          $needsProps = $needs.PSObject.Properties
          $missing = @()
          $rootCauseFailures = @()
          $cascaded = @()

          $vipStatus = $null
          $vipStatusPath = Join-Path -Path $PWD -ChildPath 'vip-build-status/vip-build.json'
          if (Test-Path $vipStatusPath) {
            try {
              $vipStatus = Get-Content -Raw -Path $vipStatusPath | ConvertFrom-Json
            } catch {
              Write-Warning "Failed to parse VIP build status file at ${vipStatusPath}: $($_.Exception.Message)"
            }
          }

          function Get-JobEntryLabel {
            param(
              [string]$JobName,
              [string]$Result,
              [object]$VipStatusObject
            )

            if ($JobName -eq 'build-vip' -and $VipStatusObject) {
              $label = if ($VipStatusObject.reason) { $VipStatusObject.reason } elseif ($VipStatusObject.status) { $VipStatusObject.status } else { $null }
              if ($label) {
                return "{0}:{1} ({2})" -f $JobName, $Result, $label
              }
            }

            return "{0}:{1}" -f $JobName, $Result
          }

          foreach ($job in $required) {
            $entryProp = $needsProps[$job]
            if (-not $entryProp) {
              $missing += "$($job):missing"
              continue
            }
            $result = $entryProp.Value.result
            if ($result -ne 'success') {
              $entry = Get-JobEntryLabel -JobName $job -Result $result -VipStatusObject $vipStatus
              if ($result -in @('failure', 'cancelled', 'timed_out', 'action_required', 'startup_failure')) {
                $rootCauseFailures += $entry
              } elseif ($result -eq 'skipped') {
                $cascaded += $entry
              } else {
                $rootCauseFailures += $entry
              }
            }
          }

          $requiredJobVerdict = if ($missing.Count -eq 0 -and $rootCauseFailures.Count -eq 0 -and $cascaded.Count -eq 0) { 'pass' } else { 'fail' }
          $rootCauseLabel = if ($rootCauseFailures.Count -gt 0) { $rootCauseFailures -join ', ' } else { '<none>' }
          $cascadedLabel = if ($cascaded.Count -gt 0) { $cascaded -join ', ' } else { '<none>' }
          $missingLabel = if ($missing.Count -gt 0) { $missing -join ', ' } else { '<none>' }

          Write-Host ("Required job verdict: {0}" -f $requiredJobVerdict)
          Write-Host ("Root-cause failures: {0}" -f $rootCauseLabel)
          Write-Host ("Cascaded/skipped jobs: {0}" -f $cascadedLabel)
          Write-Host ("Missing required jobs: {0}" -f $missingLabel)

          if ($requiredJobVerdict -eq 'fail') {
            Write-Error ("Pipeline contract failed: required-job verdict={0}; root-cause failures={1}; cascaded/skipped={2}; missing={3}" -f $requiredJobVerdict, $rootCauseLabel, $cascadedLabel, $missingLabel)
          } else {
            Write-Host 'Pipeline contract satisfied.'
          }

  required-context:
    name: CI Required / Lint+Contract
    if: always()
    needs:
      - docs-lint
      - powershell-lint
      - pipeline-contract
    runs-on: ubuntu-latest
    steps:
      - name: Evaluate canonical required check context
        shell: pwsh
        run: |
          $docsLintResult = '${{ needs.docs-lint.result }}'
          $lintResult = '${{ needs.powershell-lint.result }}'
          $contractResult = '${{ needs.pipeline-contract.result }}'

          Write-Host ("Documentation Lint result: {0}" -f $docsLintResult)
          Write-Host ("PowerShell Lint result: {0}" -f $lintResult)
          Write-Host ("Pipeline Contract result: {0}" -f $contractResult)

          $failing = @()
          if ($docsLintResult -ne 'success') {
            $failing += "docs-lint:$docsLintResult"
          }
          if ($lintResult -ne 'success') {
            $failing += "powershell-lint:$lintResult"
          }
          if ($contractResult -ne 'success') {
            $failing += "pipeline-contract:$contractResult"
          }

          if ($failing.Count -gt 0) {
            throw ("CI required context failed because prerequisite checks were not successful: {0}" -f ($failing -join ', '))
          }

          Write-Host 'CI required context passed.'
